# Instructions Claude Code et autres agents IA

**Version documentaire : 1.48.2**
**Dernière mise à jour : 2026-09-14**

Avant toute modification, lire dans cet ordre :

1. `package.json` — relever la version courante.
2. `CHANGELOG.md` — comprendre la dernière release.
3. `docs/VERSIONING.md` — appliquer la règle de versioning.
4. `docs/BUSINESS_RULES.md`
5. `docs/DATA_AND_FORMS.md`
6. `docs/DATA_CHANGE_INSTRUCTIONS.md`
7. `docs/QE_QA_PROCESS.md`
8. `docs/QA_SCENARIOS.md`
9. `docs/CLAUDE_HANDOFF_STAFF_ACCESS.md` pour tout changement de rôles ou de `/staff`
10. `README.md`

Avant de coder, rechercher les anciennes valeurs ou règles susceptibles d'être devenues obsolètes. Après la modification, vérifier que le code, les migrations et tous les documents concernés décrivent le même état.

En présence d'un bug (signalé par l'utilisateur ou constaté), suivre `docs/QE_QA_PROCESS.md` : reproduire avec les vraies données, distinguer bug de code (script/page) et bug de données déjà en base, chercher les cas similaires non signalés par une requête groupée avant de corriger cas par cas, écrire un test de régression avant de considérer le bug clos.

Dans chaque PR, indiquer explicitement : `Version: X.Y.Z → A.B.C` ou `Version inchangée: X.Y.Z`, les fichiers documentaires mis à jour, les tests exécutés et les éventuelles migrations.

Les permissions sont centralisées dans `lib/permissions.ts`. Ne recréez pas de listes de rôles dispersées si une capacité existe déjà. Ajoutez ou modifiez la capacité, ses tests et la documentation dans le même lot/version.

Ne modifiez jamais Supabase ou Google Sheets en production sans autorisation explicite, aperçu des impacts, sauvegarde et procédure de retour arrière. Toute modification manuelle de production doit être reflétée dans une migration GitHub et dans le changelog de la version correspondante.

## État de référence v1.31.1

- 41 tables : 40 normales (1-40) + une réserve (41).
- Capacité officielle : 400 places; capacité absolue : 410.
- Session maximale : 12 h.
- Une nouvelle version déployée invalide les sessions issues d'un ancien déploiement à la prochaine requête protégée.
- Le service worker ne doit pas servir d'anciens assets Next.js `/_next/*` depuis le cache.
- `main` contient les commits `e37eb7b` (v1.29.1) et `006ce76` (v1.29.2).
- La migration `0038_strict_guest_approval_assignment.sql` a déjà été exécutée et vérifiée en production Supabase le 31/08/2026 : la fonction `assign_table_to_guest_approval_strict` existe avec `security_type = INVOKER`. Ne pas la réexécuter manuellement sans raison; conserver le fichier dans Git comme historique reproductible.
- **Les migrations `0036_enable_rls_user_credential_backups.sql` et `0037_guest_approval_app_push.sql` (déjà dans le dépôt depuis longtemps) n'avaient jamais été appliquées en production avant le 13/09/2026** — écart découvert en diagnostiquant le bug "Approuver/Refuser ne fait rien" (contrainte `decided_via` toujours limitée à `'web'`/`'whatsapp'`). Appliquées ce jour-là (RLS sur `user_credential_backups`, `'app'` ajouté à `guest_approval_requests_decided_via_check`, table `push_subscriptions` créée). Toujours vérifier `supabase list_migrations` contre les fichiers du dépôt en cas de doute — un fichier de migration présent dans Git n'est pas une garantie qu'il a été appliqué.
- `/approbations` ouvre la photo en grand et permet Approuver/Refuser. Dans l'application, `admin`, `directeur` et `visibilite` peuvent ensuite choisir la table ou laisser l'assignation au `placeur`; SMS/WhatsApp reste limité à Oui/Non.
- Une table pleine impose une réorganisation atomique : seuls les groupes avec `nombre_arrive = 0` peuvent être déplacés, et la fonction SQL refuse toute capacité cible ou destination dépassée.
- Les placeurs abonnés reçoivent un Push après approbation puis après assignation de la table. Ces notifications sont best-effort et ne conditionnent jamais la décision ou la transaction SQL.
- Navigation admin v1.29.2 : Approbations reste dans le menu du compte sur toutes les pages. Sur `/dashboard` seulement, la barre du bas est Recherche, Plan, Scan central, Agenda, Approbations. Hors dashboard, Approbations quitte la barre et Bord reprend le raccourci.
- Navigation v1.30.0 : pour admin/directeur, `/dashboard` utilise Scan au centre; `/scan` utilise Bord au centre. L’agenda partagé est stocké dans `agenda_items` et modifiable uniquement via la capacité `manageAgenda`.
- v1.30.1 : `ensure_invitation_member_rows` restaure les lignes nominatives manquantes sans modifier `nombre_prevu`/`nombre_arrive`. `manageTags` appartient à admin et directeur uniquement.
- v1.30.2 : la fiche d'approbation se ferme par un X; après approbation, Placement ouvre uniquement les tables ayant assez de places libres, avec priorité à la table 41.
- v1.31.0 : les activités de l'agenda sont entièrement modifiables; Nelly obtient l'exception nominative `users.agenda_manager` sans élargir les droits de tous les placeurs.
- v1.31.1 : Nelly est promue au rôle complet `directeur`, comme Rémy; l'application n'utilise plus l'exception nominative d'agenda.
- v1.33.1 : `app/api/agenda/route.ts` normalise `custom_assignees` en tableau même si la colonne venait à manquer côté base (`select('*')` omet silencieusement une colonne manquante — un spread sur `undefined` plantait toute `/agenda`, capturé par le filet générique `app/error.tsx` qui déconnecte l'utilisateur quelle que soit la vraie cause). `GET /api/guest-approvals?count=pending` porte désormais `Cache-Control: private, no-store` (comme la liste complète) et les trois appelants (`AccountMenu`, `BottomNav`, `GuestApprovalsShortcut`) passent `{ cache: 'no-store' }` — le badge d'approbations en attente est réellement en temps réel, plus figé par le cache HTTP du navigateur.
- Les migrations `0043_agenda_custom_assignees.sql` et `0044_guest_approval_pre_approval_reservation.sql` ont été exécutées et vérifiées en production Supabase le 02/09/2026 : `agenda_items.custom_assignees`, `guest_approval_requests.reserved_table_id`, et les fonctions `reserve_table_for_guest_approval`/`release_guest_approval_reservation` (toutes deux `SECURITY INVOKER`) existent en base. Ne pas les réexécuter manuellement sans raison; conserver les fichiers dans Git comme historique reproductible.
- v1.34.0 : `auto_assign_table_for_guest_approval` (`0045_auto_assign_table_for_guest_approval.sql`, exécutée et vérifiée en production Supabase le 02/09/2026, `SECURITY INVOKER`) place automatiquement une demande approuvée sans réservation préalable — table excédentaire en priorité, sinon la table la plus libre du même côté que l'invité, sinon de l'autre côté, sinon approuvée sans table. `/approbations` ne propose plus de réserver une table manuellement comme parcours principal (retour de Gersom : « je n'ai pas besoin de voir réserver une table directement... être capable de approuver ou refuser rapidement ») ; le mécanisme de réservation de 0044 reste fonctionnel sous le capot. `app/globals.css` : `.card`/`.action-row`/`.action-row-muted`/`.btn-secondary` reprennent le thème « verre liquide » (flou + saturation + reflet + `var(--elev-2)`) déjà utilisé par la barre du bas, dans les deux thèmes — ne pas revenir à `shadow-card` (`var(--elev-1)`, `none` en Maison).
- v1.35.0 : `linked_invitation_id` (`0046_guest_approval_linked_invitation.sql`, exécutée et vérifiée en production Supabase le 02/09/2026) lie une demande d'invité surprise à l'invitation du groupe avec qui la personne est arrivée — nouveau point d'entrée sur `/checkin/[invitationId]` (« 📷 Invité surprise », réservé à `submitGuestApproval`), côté préempli depuis l'invitation. `auto_assign_table_for_guest_approval` gagne une priorité 0 : la table de ce groupe si elle a de la place, avant même la table excédentaire. **`app/api/members/add-unplanned/route.ts` exige désormais `submitGuestApproval` au lieu de `checkin`** (retour de Gersom : « les scanners ne vont même pas traiter votre demande... c'est les placeurs qui vont gérer le reste ») — `agent_checkin` ne peut plus ajouter d'invité non prévu, ni par ce bouton ni par le nouveau parcours photo ; `set-arrival-status` (check-in normal) reste inchangé sur `checkin` pour tous.
- v1.36.0 : `+ Invité` devient un bouton rond en verre (`components/AddInvitationButton.tsx`, `.glass-icon-button` dans `app/globals.css` — même recette réutilisée pour la flèche Retour de `TopBar`), présent sur `/plan-table` ET `/dashboard`, toujours réservé à `addInvitation` (admin/directeur). `/admin/users` : bascule Actif/Désactivé en `.glass-toggle` (verre liquide) et changement de rôle/accès possible depuis la fiche d'édition — `PATCH /api/admin/users` exige le nouvel identifiant (email+mot de passe ou PIN) quand `role` change et efface le mode devenu obsolète, aucune migration nécessaire (`users_role_check`, 0015, accepte déjà les cinq rôles).
- v1.37.0 : `/agenda` remplace la longue liste de cases à cocher « Responsables » par `components/ResponsablePicker.tsx`, une fiche de recherche plein écran (équipe affichée par défaut, invités recherchés à partir de 2 caractères via `ilike` sur `invitations.nom_affichage`, jamais toute la liste chargée d'un coup). Toujours les mêmes deux champs (`assignee_ids`, `custom_assignees`), aucune migration.
- v1.38.0 : les réponses publiques d'approbation sont privées et non mises en cache; le check-in utilise les capacités centralisées; la réinitialisation de test est atomique via la migration `0047_atomic_reset_test_event_data.sql`.
- v1.38.1 : sur `/agenda`, les raccourcis latéraux `Agenda` et `Bord` sont inversés; `/dashboard` et `/scan` restent inchangés.
- v1.39.0 : la création d'une activité permet de choisir les responsables dès la modale « Nouvelle activité », avec les mêmes comptes, invités recherchés et noms libres que la modification.
- v1.39.1 : les approbations et abonnements push utilisent l'événement de la session; les lectures d'approbations sont sans cache et l'activation push gère iOS installé et Android.
- v1.39.2 : deux corrections d'affichage/navigation, aucune migration. (1) Le flash de navigation entre deux fiches d'une même route dynamique (`/table/[tableId]`, `/tables/[tableId]`, `/checkin/[invitationId]`, `/checkin/[invitationId]/members`) est corrigé : chaque page réinitialise son état au tout début de l'effet gardé sur le paramètre d'URL, avant de relancer la requête (Next.js réutilise l'instance de composant, l'état ne se réinitialise jamais tout seul). (2) Le "+" de `GuestArrivalPanel` (« Qui est arrivé ? ») appelle désormais `add_unplanned_arrival` au lieu de `add_invitation_member` — la personne ajoutée est nommée ET marquée arrivée immédiatement (déclenche l'assignation de table de réserve en cas de dépassement), réservé à la nouvelle capacité `canAdd` (= `submitGuestApproval`, distincte de `canManage` = `manageMembers` toujours réservée au renommage). L'ancien bouton autonome « + Non prévu » et le lien « Gérer les membres du groupe » ont disparu de `/checkin/[invitationId]` (consolidés dans ce "+" ; la route `/checkin/[invitationId]/members` reste fonctionnelle par URL directe). « 📷 Invité surprise » est inchangé. `/plan-table` reflète maintenant les arrivées ajoutées ainsi, sans changement de code de son côté (son abonnement temps réel était déjà correct).
- v1.40.0 : optimisations « jour J » + version visible sur le splash, aucune migration. (1) Temps réel : les UPDATE `postgres_changes` sont appliqués localement (`lib/realtimeDelta.ts`) par `/dashboard`, `/plan-table`, `/exceptions`, `/tables/[tableId]` (+ ancienne route `/table/[tableId]`) et `GuestArrivalPanel` au lieu d'un refetch complet ; INSERT/DELETE restent debouncés à 400 ms ; la souscription `guests` du panneau est filtrée localement par ids déjà listés. (2) `hooks/usePolling.ts` suspend les intervalles (`AccountMenu`, `GuestApprovalsShortcut`, `BottomNav`, `/staff`, `/agenda`, `/approbations`) quand l'onglet est masqué. (3) `lib/supabase/client.ts` : singleton navigateur par onglet. (4) `/search` : dataset complet chargé seulement en mode « parcourir », colonnes réduites ; `/tables/move-multiple` : une requête `Promise.all` ; `xlsx` en import dynamique sur les pages admin ; `poweredByHeader: false`. (5) `app/page.tsx` lit `package.json` (`version`) et le passe à `SplashScreen` (badge « v1.40.0 » en bas à droite).
- v1.41.0 : forçage d'assignation d'un invité surprise sur une table pleine (migration `0049`, `p_force` ajouté à `assign_table_to_guest_approval_strict`, défaut false = comportement strict inchangé ; capacité cible non bloquante si forcé, destinations d'une réorganisation restent strictes, arrivés jamais déplacés, tracé dans `audit_logs`). Toggle « Forcer le placement » sur `/approbations/[id]/assign`. Correctif test `navigation-resilience.test.ts` (fenêtre `{0,200}` → `{0,400}`).
- v1.41.1/v1.41.2/v1.41.3 : alerte CodeQL ("DOM text reinterpreted as HTML", `js/xss-through-dom`) sur l'aperçu photo de `components/GuestApprovalCaptureFlow.tsx` (`preview`, alimenté par `URL.createObjectURL(photo)`, rendu comme `src` d'`<img>`). Garde-fou de type `safePreview` (préfixe `blob:` whitelist) ajouté en v1.41.1 — CodeQL ne le reconnaît pas comme sanitizer pour cette requête, l'alerte reste levée. Le commentaire de suppression inline documenté par GitHub (`codeql[js/xss-through-dom]`) tenté en v1.41.2 (deux placements) n'a lui non plus aucun effet : **ce repo utilise le Default setup CodeQL (aucun `.github/workflows/*codeql*` versionné)**, qui ne semble pas traiter ces commentaires — à vérifier si un jour le repo passe en Advanced setup. Alerte rejetée manuellement par Gersom comme faux positif dans l'onglet Security → Code scanning de GitHub (04/09/2026) ; le garde-fou `safePreview` reste en code (v1.41.3 retire le commentaire de suppression inefficace/trompeur, sans effet réel sur le comportement).
- v1.42.0 : lot de 8 correctifs/fonctionnalités (retour de Gersom, 4 photos, 13/09/2026). **Bug critique corrigé** : Approuver/Refuser ne faisait rien dans l'app ("La décision n'a pas pu être enregistrée") — la contrainte `guest_approval_requests_decided_via_check` en production n'autorisait jamais `'app'` (seulement `'web'`/`'whatsapp'`), alors que la décision app envoie toujours `'app'`. Root-cause trouvée par reproduction directe en base (transaction BEGIN/ROLLBACK). La migration `0037_guest_approval_app_push.sql` (déjà dans le dépôt depuis longtemps mais jamais appliquée en production — écart entre `supabase migrations list` et l'historique réel du dépôt) corrige exactement ça ; **appliquée en production le 13/09/2026**, avec au passage `push_subscriptions` (table manquante depuis toujours, notifications Web Push muettes sans erreur visible) et `0036_enable_rls_user_credential_backups.sql` (RLS manquante sur une table inutilisée, signalée par l'advisor Supabase). Aucune nouvelle migration écrite : ces deux fichiers existaient déjà tels quels. **Bottom nav** : Approbations remplace Agenda (barre générique admin/directeur hors `/dashboard`/`/scan`/`/agenda`) et Staff (visibilite) en dernière position. **Flash corrigé** dans `GuestArrivalPanel` (état `loading`/`members` de l'invitation précédente affiché brièvement en changeant d'invitation, ex: solo → groupe) : réinitialisé synchroniquement au changement de `invitation.id`. **`/checkin/[invitationId]`** : bloc "Table N" et étiquette `T0XX` cliquables vers `/tables/[tableId]`. **Caméra en direct** (`components/PhotoCaptureCamera.tsx`, `getUserMedia` + capture canvas, même mécanisme que `QrScanner.captureFrame`) remplace l'`<input capture>` qui ouvrait l'app Camera native pour "📷 Invité surprise". **"Arrivé(e) avec"** (`linked_invitation_id`) affiché dans `/approbations` (liste + détail) et sur `/approbations/[id]/assign`, où sa table est désormais **présélectionnée automatiquement** (priorité avant la table 41), modifiable en touchant une autre table. Fiche détaillée de `/approbations` resserrée (photo 42dvh → 26dvh).
- v1.42.1 : badge de version du splash déplacé (retour de Gersom : "c'est trop en bas à droite... une translation d'à peu près 1 cm à l'angle 315°") — `transform: translate(-0.71cm, -0.71cm)` (cap boussole 315° = nord-ouest) en plus de l'ancrage `bottom-3 right-4` existant, remonte le badge vers le centre au lieu de le laisser collé dans l'angle. La synchronisation automatique avec `package.json` (déjà en place depuis v1.40.0) est inchangée et reconfirmée.
- v1.43.0 : deuxième lot du 13/09/2026 (retour de Gersom, 5 photos). **Refus reconsidérable** : `applyGuestApprovalDecision` (lib/guestApprovalDecide.ts) accepte un 6e paramètre `allowReconsiderFromRefused` (vrai uniquement depuis `/api/guest-approvals/[id]/decide`, jamais sur `/approve/[token]` ni WhatsApp) qui autorise `refuse -> approuve` (jamais l'inverse) — bouton "Reconsidérer → Approuver" sur la liste et la fiche détaillée de `/approbations`. **Flash résiduel de `GuestArrivalPanel` corrigé** : la branche "ensure" (backfill d'une invitation jamais matérialisée, typiquement rencontrée depuis `/plan-table`) ne marquait pas `initializing=true` avant son fetch, contrairement à la branche "initialize" juste au-dessus — fenêtre où `settled=true` et `visible=false` faisait flasher l'ancien compteur. **Pincement cassé sur `/plan-table` après rotation** : le tire-pour-rafraîchir (Touch Events) et le zoom du plan (Pointer Events, `ZoomableFloorPlan.tsx`) vivaient sur le même conteneur et se disputaient les gestes à deux doigts — les deux gestionnaires de tire-pour-rafraîchir ignorent désormais `e.touches.length > 1`. **Swipe pour supprimer** (admin uniquement) une demande déjà décidée sur `/approbations` (`components/SwipeableDeleteCard.tsx` + `DELETE /api/guest-approvals/[id]`, refuse toute demande `en_attente`). **Texte de `/approbations/[id]/assign` raccourci** en un badge "Automatique"/"Sélectionnée". **agent_checkin** gagne `viewGuestApprovals` en lecture seule (jamais `reviewGuestApproval` ni `assignGuestApproval` — ce rôle continue de renvoyer vers un placeur) : Approbations remplace Agenda en dernier onglet de la barre du bas (4 emplacements seulement, pas de place pour les deux) ; l'agenda du jour reste visible sur `/scan` via `NextAgendaActivity`, déjà affiché pour ce rôle sans onglet dédié.
- v1.43.1 : `components/PushNotificationButton.tsx` affiche désormais des instructions précises par plateforme (iOS/Android détectés séparément) quand une action manuelle est nécessaire (refus explicite, ou iOS pas encore installé sur l'écran d'accueil), au lieu d'un message générique "à configurer" — retour de Gersom : "j'aimerais que ça m'amène directement dans les settings... un peu comme le prompt caméra". Aucune API web ne permet d'ouvrir directement les réglages système de notifications d'une PWA sur iOS ou Android (contrairement à `getUserMedia`/caméra, qui a sa propre invite native depuis la page) — restriction de plateforme documentée dans le code, les instructions pas-à-pas sont le meilleur substitut possible.
- v1.44.0 : reconsidérer un refus (v1.43.0) mène désormais d'abord au choix d'une table, avant l'approbation — retour de Gersom : "je n'avais pas l'option de le mettre sur une table... le process a été automatique". "Reconsidérer → choisir une table" (liste et fiche détaillée de `/approbations`) navigue vers `/approbations/[id]/assign`, qui gagne un troisième mode `reconsider` (statut `refuse`) en plus de `reserve`/`assign` : même écran de choix de table que la réservation avant-décision, mais valide via la nouvelle route `POST /api/guest-approvals/[id]/reconsider-assign` (exige `reviewGuestApproval` ET `assignGuestApproval`) qui réserve la table choisie puis approuve (`allowReconsiderFromRefused`) en une seule action — si la réservation échoue (table pleine entre-temps), rien n'est décidé. `reserve_table_for_guest_approval` (migration `0050_reserve_table_for_reconsidered_refusal.sql`, exécutée et vérifiée en production Supabase le 13/09/2026, `SECURITY INVOKER`) accepte désormais aussi le statut `refuse`, pas seulement `en_attente` — une demande refusée n'a jamais de `reserved_table_id` déjà posé (libéré automatiquement au refus), aucun risque de collision.
- v1.45.0 : deux correctifs (retour de Gersom, capture d'écran Agent001 sur `/scan`). (1) **Navigation `agent_checkin`** : "les agents scan sont là seulement pour scanner et non pour approuver — s'il y a un problème ils redirigent vers le placeur qui fait la photo. Le bouton au milieu devrait être tableau de bord, à droite agenda puis approbation." `AGENT_CHECKIN_ITEMS` (`components/BottomNav.tsx`) devient Recherche, Plan, **Tableau de bord** (`CENTRAL_HREF['agent_checkin'] = '/dashboard'`), Agenda, Approbations — plus d'onglet Scan dédié : `/scan` reste la page d'atterrissage par défaut de ce rôle (`landingPathForRole`) et reste joignable via la flèche Retour du TopBar (`/approbations` → `/scan` direct, `/agenda` → `/dashboard` → `/scan`). (2) **"Ça ne reste pas figé" pendant le défilement, bouton central injoignable ensuite — "make it really fixed like in an app"** : `hooks/usePullToRefresh.ts` (utilisé par `/dashboard`) gardait "seulement en haut de page" avec `window.scrollY <= 0`, structurellement toujours vrai dans cette app (page ancrée, seul un `<div overflow-y-auto>` interne défile) — le geste "tirer pour actualiser" pouvait donc se déclencher, et rester bloqué (aucun `touchcancel`), au milieu d'un défilement normal. Corrigé en vérifiant le `scrollTop` du vrai conteneur défilant (`containerRef`, comme `/plan-table` le faisait déjà) + `touchcancel` sur les deux. Chaque écran principal passe aussi de `h-dvh` seul à `fixed inset-0` (ancré au viewport réel plutôt que dépendant d'un recalcul de hauteur pendant qu'une barre d'outils système apparaît/disparaît en défilement) — aucun changement du flex interne, `BottomNav` garde exactement sa place.
- v1.45.1 : "les deux SS qui vont par-dessus le bouton recherche" (retour de Gersom, `/scan` tourné en paysage) — le bouton de compte flottant (`AccountMenu floating`, via `components/UserMenu.tsx`, utilisé uniquement sur `/scan` et `/placement`, les deux seuls écrans sans TopBar) restait ancré en haut à droite du viewport quelle que soit l'orientation, alors qu'en paysage la barre de navigation devient une bande verticale collée à ce même bord droit (`landscape:w-20`, `components/BottomNav.tsx`) : les deux se superposaient, recouvrant Recherche. Bascule désormais à gauche uniquement en paysage (`landscape:left-[calc(1rem+env(safe-area-inset-left))]`, portrait inchangé) ; son panneau déroulant s'ouvre vers la droite dans ce cas pour rester à l'écran. La bannière "Nouvelle approbation" du même composant, qui passait sous cette même bande en paysage, s'arrête désormais avant elle. L'usage non flottant (`TopBar`, toutes les autres pages) n'a jamais ce problème (déjà calé par le flux flex), aucun changement là.
- v1.45.2 : "à déréguler ce problème de app fixé même quand on met en horizontal" (retour de Gersom, `/plan-table` tourné en paysage sur iPad) — le dernier contenu visible (dernière rangée de tables) et le dernier onglet de la barre de navigation verticale pouvaient se retrouver recouverts par la barre gestuelle système, qui reste horizontale au bas de l'écran même en paysage sur iPad (contrairement à l'iPhone, où elle rejoint un des côtés). `.bottom-nav-glass` réserve déjà cet espace en portrait (`margin-bottom`), mais le remet explicitement à 0 en paysage (elle devient une bande verticale, protégée seulement côté `safe-right`) — rien ne protégeait le bord du bas, probablement masqué avant `fixed inset-0` (v1.45.0) par un calcul `h-dvh` qui excluait déjà cette zone sur certains navigateurs, exposé une fois le viewport réel utilisé directement. Corrigé en réduisant le rectangle de la coquille de chaque page en paysage (`landscape:bottom-[env(safe-area-inset-bottom)]` au lieu de `bottom-0`, sur les 11 pages utilisant `BottomNav`) : protège d'un coup le contenu ET le dernier onglet de la bande verticale, sans toucher au padding interne d'aucune page individuellement.
- v1.46.0 : la nav de `placeur` calque désormais le comportement contextuel de `directeur` — retour de Gersom sur Agent001 (comparé à Rémy Landu) : "je m'attends plutôt à voir un menu... un peu comme celui de Rémy Landu". Rôle vérifié directement en base avant de coder (`select role from users`) : Agent001 est bien `placeur`, pas `agent_checkin` comme les échanges précédents sur ce compte le laissaient penser — ce correctif ne touche en rien `agent_checkin` (déjà traité les 03 et 13/09/2026). Question posée à Gersom entre un simple remplacement du dernier onglet (Staff → Approbations, comme pour les autres rôles) et la reproduction complète du comportement de directeur ; il a choisi la seconde option. `components/BottomNav.tsx` : `isAdminDirector` devient `isDirectorStyleNav` (`role === 'admin' || role === 'directeur' || role === 'placeur'`) — Tableau de bord au centre hors `/dashboard`/`/scan`/`/agenda` (`CENTRAL_HREF.placeur = '/dashboard'`, `ITEMS.placeur` reprend exactement la forme de `ITEMS.directeur` : Recherche, Plan, Bord, Scan, Approbations, `STAFF_ITEMS` supprimée car devenue inutilisée), Scan/appareil photo au centre sur ces trois pages avec Agenda visible à côté. `lib/permissions.ts` : `placeur` gagne `viewAgenda` en lecture seule (jamais `manageAgenda`), nécessaire pour que ce nouvel onglet Agenda soit réellement accessible (même principe que le `viewAgenda` d'`agent_checkin`, v1.40.0) — `/staff` reste atteignable via le badge QR "STAFF" depuis `/scan` (`viewStaff` inchangée).
- v1.46.1 : `agent_checkin` (Scotty Sanda) rejoint à son tour `isDirectorStyleNav`, même jour — retour de Gersom : "quand on est dans le tableau de bord, je voudrais que le bouton doré en bas soit le bouton scan plutôt". Le bouton central de ce rôle (Bord, `CENTRAL_HREF.agent_checkin`) pointait vers `/dashboard` même en y étant déjà, un aller-retour inutile déjà corrigé pour admin/directeur le 02/09/2026. Scan (jamais l'appareil photo, ce rôle n'a pas `submitGuestApproval`) devient le centre sur `/dashboard`/`/scan`/`/agenda`, Bord ailleurs — `AGENT_CHECKIN_ITEMS` (barre générique) inchangé.
- v1.47.0 : mise à jour ciblée de la liste d'invités depuis `guest-list_48.csv` (export With Joy le plus récent transmis par Gersom le 14/09/2026, "tu as tous les droits... on ne change pas toute la base, juste quelques changements") + nouvelle table 42. **Table 41 renommée « Houston » et devenue régulière ; nouvelle table 42 « Johannesburg » devient l'unique réserve** (capacité 10, confirmée par Gersom), migration `0051_table_42_johannesburg_reserve.sql` appliquée et vérifiée en production le jour même — nouvelle structure : 42 tables, 410 places officielles, 420 places absolues (`lib/withjoyImport.ts`, `app/plan-table/page.tsx`, `app/admin/import-withjoy/page.tsx`, `components/CapacityGauge.tsx` et toute la documentation versionnée mis à jour en conséquence). **Comparaison nom-par-nom** (jamais un réimport complet/destructif via `/admin/import-withjoy`, conformément à `docs/DATA_CHANGE_INSTRUCTIONS.md` section 6) entre les 252 invitations existantes et les 407 lignes du CSV, en réutilisant `lib/withjoyImport.ts` (`parseCsvText`/groupement par `party`) : **201 rafraîchissements de contact** (téléphone/email) sur des invitations déjà présentes reconnues par correspondance exacte de nom ; **19 nouvelles invitations** sans correspondance ajoutées avec leurs coordonnées, **volontairement sans table assignée** — le plan de table photographié transmis par Gersom (nouvelle configuration) montre des occupants différents de ceux actuellement en base pour plusieurs tables cibles (ex. table 29 : "Famille Vincent Nkouka" + "Tia Nzuzi Culumbu" en base, "Famille Nzasi"/"Famille Manuel" sur la photo) — une réorganisation complète des places déjà occupées était hors périmètre explicite de cette demande ; la table lue sur la photo est conservée dans les notes de chaque nouvelle invitation pour référence, jamais devinée silencieusement pour un placement effectif. **Bergine Momene et Ralph Momene** (déjà réunis dans "Famille Momene" en base) et **Tantine Angèle Lukau** (nouvelle invitation) marqués `ne_viendra_pas = true` sur confirmation explicite de Gersom ("they will not come to wedding, we will only send them after probable"). **Bug réel trouvé pendant cette revue** : `app/approbations/[id]/assign/page.tsx` codait en dur la priorité de placement sur `usage.table.number === 41` (jusqu'ici la seule réserve) — corrigé en `=== 42` puisque 41 est désormais une table régulière, sans quoi "Houston" aurait été proposée en priorité devant la vraie réserve. **Non fait dans ce lot** (explicitement hors périmètre, ou bloqué sur la fiabilité de l'OCR d'un plan photographié plutôt qu'un export machine-lisible) : redessiner `components/FloorPlan.tsx` selon la nouvelle configuration de zones (20 tables sud, 22 tables nord en 5 rangées de 4 + 2 tables ouest) — la table 42 n'apparaît donc pas encore sur ce plan visuel optionnel, bien que pleinement fonctionnelle partout ailleurs ; surbrillance des chaises par personne (explicitement laissée à discrétion par Gersom) ; réassignation des invitations déjà en base vers une nouvelle table selon la photo.
- v1.48.0 : suite directe de v1.47.0, sur demande explicite de Gersom ("Finish floor-plan visual redesign... Estimate using database to help") — feu vert donné pour utiliser l'OCR des photos malgré le risque d'erreur, en l'atténuant par recoupement avec la base plutôt qu'en attendant un export machine-lisible qui n'est jamais arrivé. **`components/FloorPlan.tsx` entièrement redessiné en deux zones (nord/sud)** reproduisant l'ordre/voisinage exact des deux photos (OCR table par table, 44 crops relus un par un, méthodique) : zone nord 5×4, zone sud 6×4 (2 cases vides = "Piste et File Attente" de la photo, déjà couverte par la salle "Piste de danse" existante). Les 42 tables ont désormais toutes une position, y compris la 42 ("Johannesburg"). **Bug réel trouvé en recroisant avec la base** : la rangée 4 de la zone nord avait les tables 10/16/17/9 sur les mauvaises colonnes (`1,10,16,17,9` au lieu de `1,9,10,16,17`) — corrigé après re-vérification. **`lib/floorPlanSeats.ts`** (nouveau) : noms de sièges par table lus sur les photos, recoupés avec les 272 invitations actuelles (nom exact + tolérance approchée) — ~87% des noms trouvent une correspondance (mêmes noms déjà ajoutés sans table en v1.47.0 pour 19 d'entre eux, confirmant que les deux extractions indépendantes — tags CSV et OCR photo — concordent), et ce recoupement a permis de corriger plusieurs lectures OCR incertaines (ex. "Jean-Ciben Ca ous" → "Jean-Clivens Le Caous", confirmé en base). **PUREMENT INFORMATIF** : jamais écrit en base, jamais une source de placement (qui reste `invitations.table_id`) — affiché comme panneau optionnel "Vu sur le plan photographié" sous la fiche de table sélectionnée sur `/plan-table`, avec sièges togglables en surbrillance (état local uniquement). Les 15 écarts identifiés entre le plan photographié et les tables actuelles en base (ex. "Furty Matuba" photo table 18 vs base table 37) ne sont volontairement PAS appliqués dans ce lot — resteraient à traiter séparément si Gersom le demande explicitement (réorganisation des places déjà occupées, hors périmètre confirmé en v1.47.0).
- v1.48.2 : retour de Gersom (5 photos) sur le panneau « Vu sur le plan photographié » de `/plan-table` — "je veux voir un dessin plutot de chaque tables avec les places... juste une image de la table avec leurs differents noms et leurs sieges et voir aussi les sieges vides. Vraiment comme sur les images" (photos seatplan.io transmises en référence) et "quand on selectionne une personne dans la liste ca doit mettre la chaise de la personne en brillance... comme sur la photo" (exemple d'une carte nominative numérique montrant un siège mis en évidence). **`components/TableSeatWheel.tsx`** (nouveau) remplace l'ancienne grille de boutons (`grid grid-cols-2`) par un vrai dessin SVG circulaire : dix étiquettes de siège rayonnant autour d'un cercle central numéroté, dans le sens horaire depuis le haut, chacune tournée à son propre angle (`transform="rotate(...)"`, même mécanisme que les libellés verticaux de `components/FloorPlan.tsx`) — fidèle aux photos transmises, y compris les étiquettes du bas qui se retrouvent donc la tête en bas (aucune correction de lisibilité). Les sièges vides restent visuellement distincts (contour pointillé, libellé "Vide"). Le siège sélectionné (état `highlightedSeat`, déjà introduit en v1.48.0) est mis en évidence avec les couleurs accent, exactement comme sur l'exemple de carte nominative montré par Gersom. `app/plan-table/page.tsx` : le panneau garde le même conteneur `.card` et le même texte informatif (complété d'une phrase "Touchez un nom pour mettre son siège en évidence."), seul son contenu change — toujours **purement informatif**, jamais une source de placement (`lib/floorPlanSeats.ts` inchangé). `tests/table-seat-wheel.test.ts` (nouveau).
- v1.48.1 : correctif du même jour, retour de Gersom sur capture d'écran de l'app rendue — "tu as inversé la zone sud et la zone nord... prends les 22 tables en bas, monte-les dans le nord, prends les 20 en haut, mets-les en bas... garder l'alignement... deux tables qui sont justement dans le nord-ouest (22 et 23)". Le bloc à 22 tables (6×4, avec la paire 22/23) et le bloc à 20 tables (5×4 "propre") étaient positionnés à l'envers en v1.48.0. Corrigé dans `components/FloorPlan.tsx` en permutant uniquement la bande de rangées Y entre les deux blocs (mêmes colonnes X qu'avant pour chaque table, donc même alignement/mêmes voisins) : aucune position redevinée, seul le bloc entier change de bande verticale. La paire 22/23 (première colonne du bloc nord, rangées 1-2) est bien au nord-ouest désormais. `FLOOR_PLAN_ZONE_LABELS` n'a pas eu besoin de changer (sa position dépend de la bande Y haut/bas, pas des tables qui s'y trouvent).


## Reprise rapide pour Claude AI

1. Commencer par `git fetch origin main` et comparer `HEAD` à `origin/main`; ne jamais supposer qu'un diff transmis est encore manquant.
2. Vérifier `package.json` : la version attendue au moment de cette transmission est `1.48.1`.
3. Pour les approbations, lire ensemble `app/approbations/page.tsx`, `app/approbations/[id]/assign/page.tsx`, `lib/guestApprovalDecide.ts`, `lib/webPush.ts` et les migrations `0038` et `0050`.
4. Pour la navigation, modifier la source centralisée `components/BottomNav.tsx`; ne pas recopier des menus dans les pages.
5. Ne pas modifier la règle des rôles sans mettre à jour `lib/permissions.ts`, les routes API, `tests/permissions.test.ts`, `tests/guest-approvals.test.ts` et `docs/BUSINESS_RULES.md` dans le même lot.
6. Avant livraison : `npx tsc --noEmit`, toutes les suites `node --test tests/*.test.ts`, `npm run build`, puis `git diff --check`.
7. Les prochains changements doivent passer par une branche et une PR pour permettre la révision avant fusion; ne pousser directement sur `main` que si Gersom le demande explicitement.
