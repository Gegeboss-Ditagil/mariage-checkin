# Instructions Claude Code et autres agents IA

**Version documentaire : 1.53.7**
**Dernière mise à jour : 2026-09-16**

Avant toute modification, lire dans cet ordre :

1. `package.json` — relever la version courante.
2. `CHANGELOG.md` — comprendre la dernière release.
3. `docs/VERSIONING.md` — appliquer la règle de versioning.
4. `docs/BUSINESS_RULES.md`
5. `docs/DATA_AND_FORMS.md`
6. `docs/DATA_CHANGE_INSTRUCTIONS.md`
7. `docs/QE_QA_PROCESS.md`
8. `docs/QA_SCENARIOS.md`
9. `docs/CLAUDE_HANDOFF_STAFF_ACCESS.md` pour tout changement de rôles ou de `/staff`
10. `README.md`

Avant de coder, rechercher les anciennes valeurs ou règles susceptibles d'être devenues obsolètes. Après la modification, vérifier que le code, les migrations et tous les documents concernés décrivent le même état.

En présence d'un bug (signalé par l'utilisateur ou constaté), suivre `docs/QE_QA_PROCESS.md` : reproduire avec les vraies données, distinguer bug de code (script/page) et bug de données déjà en base, chercher les cas similaires non signalés par une requête groupée avant de corriger cas par cas, écrire un test de régression avant de considérer le bug clos.

Dans chaque PR, indiquer explicitement : `Version: X.Y.Z → A.B.C` ou `Version inchangée: X.Y.Z`, les fichiers documentaires mis à jour, les tests exécutés et les éventuelles migrations.

Les permissions sont centralisées dans `lib/permissions.ts`. Ne recréez pas de listes de rôles dispersées si une capacité existe déjà. Ajoutez ou modifiez la capacité, ses tests et la documentation dans le même lot/version.

Ne modifiez jamais Supabase ou Google Sheets en production sans autorisation explicite, aperçu des impacts, sauvegarde et procédure de retour arrière. Toute modification manuelle de production doit être reflétée dans une migration GitHub et dans le changelog de la version correspondante.

## État de référence v1.31.1

- 41 tables : 40 normales (1-40) + une réserve (41).
- Capacité officielle : 400 places; capacité absolue : 410.
- Session maximale : 12 h.
- Une nouvelle version déployée invalide les sessions issues d'un ancien déploiement à la prochaine requête protégée.
- Le service worker ne doit pas servir d'anciens assets Next.js `/_next/*` depuis le cache.
- `main` contient les commits `e37eb7b` (v1.29.1) et `006ce76` (v1.29.2).
- La migration `0038_strict_guest_approval_assignment.sql` a déjà été exécutée et vérifiée en production Supabase le 31/08/2026 : la fonction `assign_table_to_guest_approval_strict` existe avec `security_type = INVOKER`. Ne pas la réexécuter manuellement sans raison; conserver le fichier dans Git comme historique reproductible.
- **Les migrations `0036_enable_rls_user_credential_backups.sql` et `0037_guest_approval_app_push.sql` (déjà dans le dépôt depuis longtemps) n'avaient jamais été appliquées en production avant le 13/09/2026** — écart découvert en diagnostiquant le bug "Approuver/Refuser ne fait rien" (contrainte `decided_via` toujours limitée à `'web'`/`'whatsapp'`). Appliquées ce jour-là (RLS sur `user_credential_backups`, `'app'` ajouté à `guest_approval_requests_decided_via_check`, table `push_subscriptions` créée). Toujours vérifier `supabase list_migrations` contre les fichiers du dépôt en cas de doute — un fichier de migration présent dans Git n'est pas une garantie qu'il a été appliqué.
- `/approbations` ouvre la photo en grand et permet Approuver/Refuser. Dans l'application, `admin`, `directeur` et `visibilite` peuvent ensuite choisir la table ou laisser l'assignation au `placeur`; SMS/WhatsApp reste limité à Oui/Non.
- Une table pleine impose une réorganisation atomique : seuls les groupes avec `nombre_arrive = 0` peuvent être déplacés, et la fonction SQL refuse toute capacité cible ou destination dépassée.
- Les placeurs abonnés reçoivent un Push après approbation puis après assignation de la table. Ces notifications sont best-effort et ne conditionnent jamais la décision ou la transaction SQL.
- Navigation admin v1.29.2 : Approbations reste dans le menu du compte sur toutes les pages. Sur `/dashboard` seulement, la barre du bas est Recherche, Plan, Scan central, Agenda, Approbations. Hors dashboard, Approbations quitte la barre et Bord reprend le raccourci.
- Navigation v1.30.0 : pour admin/directeur, `/dashboard` utilise Scan au centre; `/scan` utilise Bord au centre. L’agenda partagé est stocké dans `agenda_items` et modifiable uniquement via la capacité `manageAgenda`.
- v1.30.1 : `ensure_invitation_member_rows` restaure les lignes nominatives manquantes sans modifier `nombre_prevu`/`nombre_arrive`. `manageTags` appartient à admin et directeur uniquement.
- v1.30.2 : la fiche d'approbation se ferme par un X; après approbation, Placement ouvre uniquement les tables ayant assez de places libres, avec priorité à la table 41.
- v1.31.0 : les activités de l'agenda sont entièrement modifiables; Nelly obtient l'exception nominative `users.agenda_manager` sans élargir les droits de tous les placeurs.
- v1.31.1 : Nelly est promue au rôle complet `directeur`, comme Rémy; l'application n'utilise plus l'exception nominative d'agenda.
- v1.33.1 : `app/api/agenda/route.ts` normalise `custom_assignees` en tableau même si la colonne venait à manquer côté base (`select('*')` omet silencieusement une colonne manquante — un spread sur `undefined` plantait toute `/agenda`, capturé par le filet générique `app/error.tsx` qui déconnecte l'utilisateur quelle que soit la vraie cause). `GET /api/guest-approvals?count=pending` porte désormais `Cache-Control: private, no-store` (comme la liste complète) et les trois appelants (`AccountMenu`, `BottomNav`, `GuestApprovalsShortcut`) passent `{ cache: 'no-store' }` — le badge d'approbations en attente est réellement en temps réel, plus figé par le cache HTTP du navigateur.
- Les migrations `0043_agenda_custom_assignees.sql` et `0044_guest_approval_pre_approval_reservation.sql` ont été exécutées et vérifiées en production Supabase le 02/09/2026 : `agenda_items.custom_assignees`, `guest_approval_requests.reserved_table_id`, et les fonctions `reserve_table_for_guest_approval`/`release_guest_approval_reservation` (toutes deux `SECURITY INVOKER`) existent en base. Ne pas les réexécuter manuellement sans raison; conserver les fichiers dans Git comme historique reproductible.
- v1.34.0 : `auto_assign_table_for_guest_approval` (`0045_auto_assign_table_for_guest_approval.sql`, exécutée et vérifiée en production Supabase le 02/09/2026, `SECURITY INVOKER`) place automatiquement une demande approuvée sans réservation préalable — table excédentaire en priorité, sinon la table la plus libre du même côté que l'invité, sinon de l'autre côté, sinon approuvée sans table. `/approbations` ne propose plus de réserver une table manuellement comme parcours principal (retour de Gersom : « je n'ai pas besoin de voir réserver une table directement... être capable de approuver ou refuser rapidement ») ; le mécanisme de réservation de 0044 reste fonctionnel sous le capot. `app/globals.css` : `.card`/`.action-row`/`.action-row-muted`/`.btn-secondary` reprennent le thème « verre liquide » (flou + saturation + reflet + `var(--elev-2)`) déjà utilisé par la barre du bas, dans les deux thèmes — ne pas revenir à `shadow-card` (`var(--elev-1)`, `none` en Maison).
- v1.35.0 : `linked_invitation_id` (`0046_guest_approval_linked_invitation.sql`, exécutée et vérifiée en production Supabase le 02/09/2026) lie une demande d'invité surprise à l'invitation du groupe avec qui la personne est arrivée — nouveau point d'entrée sur `/checkin/[invitationId]` (« 📷 Invité surprise », réservé à `submitGuestApproval`), côté préempli depuis l'invitation. `auto_assign_table_for_guest_approval` gagne une priorité 0 : la table de ce groupe si elle a de la place, avant même la table excédentaire. **`app/api/members/add-unplanned/route.ts` exige désormais `submitGuestApproval` au lieu de `checkin`** (retour de Gersom : « les scanners ne vont même pas traiter votre demande... c'est les placeurs qui vont gérer le reste ») — `agent_checkin` ne peut plus ajouter d'invité non prévu, ni par ce bouton ni par le nouveau parcours photo ; `set-arrival-status` (check-in normal) reste inchangé sur `checkin` pour tous.
- v1.36.0 : `+ Invité` devient un bouton rond en verre (`components/AddInvitationButton.tsx`, `.glass-icon-button` dans `app/globals.css` — même recette réutilisée pour la flèche Retour de `TopBar`), présent sur `/plan-table` ET `/dashboard`, toujours réservé à `addInvitation` (admin/directeur). `/admin/users` : bascule Actif/Désactivé en `.glass-toggle` (verre liquide) et changement de rôle/accès possible depuis la fiche d'édition — `PATCH /api/admin/users` exige le nouvel identifiant (email+mot de passe ou PIN) quand `role` change et efface le mode devenu obsolète, aucune migration nécessaire (`users_role_check`, 0015, accepte déjà les cinq rôles).
- v1.37.0 : `/agenda` remplace la longue liste de cases à cocher « Responsables » par `components/ResponsablePicker.tsx`, une fiche de recherche plein écran (équipe affichée par défaut, invités recherchés à partir de 2 caractères via `ilike` sur `invitations.nom_affichage`, jamais toute la liste chargée d'un coup). Toujours les mêmes deux champs (`assignee_ids`, `custom_assignees`), aucune migration.
- v1.38.0 : les réponses publiques d'approbation sont privées et non mises en cache; le check-in utilise les capacités centralisées; la réinitialisation de test est atomique via la migration `0047_atomic_reset_test_event_data.sql`.
- v1.38.1 : sur `/agenda`, les raccourcis latéraux `Agenda` et `Bord` sont inversés; `/dashboard` et `/scan` restent inchangés.
- v1.39.0 : la création d'une activité permet de choisir les responsables dès la modale « Nouvelle activité », avec les mêmes comptes, invités recherchés et noms libres que la modification.
- v1.39.1 : les approbations et abonnements push utilisent l'événement de la session; les lectures d'approbations sont sans cache et l'activation push gère iOS installé et Android.
- v1.39.2 : deux corrections d'affichage/navigation, aucune migration. (1) Le flash de navigation entre deux fiches d'une même route dynamique (`/table/[tableId]`, `/tables/[tableId]`, `/checkin/[invitationId]`, `/checkin/[invitationId]/members`) est corrigé : chaque page réinitialise son état au tout début de l'effet gardé sur le paramètre d'URL, avant de relancer la requête (Next.js réutilise l'instance de composant, l'état ne se réinitialise jamais tout seul). (2) Le "+" de `GuestArrivalPanel` (« Qui est arrivé ? ») appelle désormais `add_unplanned_arrival` au lieu de `add_invitation_member` — la personne ajoutée est nommée ET marquée arrivée immédiatement (déclenche l'assignation de table de réserve en cas de dépassement), réservé à la nouvelle capacité `canAdd` (= `submitGuestApproval`, distincte de `canManage` = `manageMembers` toujours réservée au renommage). L'ancien bouton autonome « + Non prévu » et le lien « Gérer les membres du groupe » ont disparu de `/checkin/[invitationId]` (consolidés dans ce "+" ; la route `/checkin/[invitationId]/members` reste fonctionnelle par URL directe). « 📷 Invité surprise » est inchangé. `/plan-table` reflète maintenant les arrivées ajoutées ainsi, sans changement de code de son côté (son abonnement temps réel était déjà correct).
- v1.40.0 : optimisations « jour J » + version visible sur le splash, aucune migration. (1) Temps réel : les UPDATE `postgres_changes` sont appliqués localement (`lib/realtimeDelta.ts`) par `/dashboard`, `/plan-table`, `/exceptions`, `/tables/[tableId]` (+ ancienne route `/table/[tableId]`) et `GuestArrivalPanel` au lieu d'un refetch complet ; INSERT/DELETE restent debouncés à 400 ms ; la souscription `guests` du panneau est filtrée localement par ids déjà listés. (2) `hooks/usePolling.ts` suspend les intervalles (`AccountMenu`, `GuestApprovalsShortcut`, `BottomNav`, `/staff`, `/agenda`, `/approbations`) quand l'onglet est masqué. (3) `lib/supabase/client.ts` : singleton navigateur par onglet. (4) `/search` : dataset complet chargé seulement en mode « parcourir », colonnes réduites ; `/tables/move-multiple` : une requête `Promise.all` ; `xlsx` en import dynamique sur les pages admin ; `poweredByHeader: false`. (5) `app/page.tsx` lit `package.json` (`version`) et le passe à `SplashScreen` (badge « v1.40.0 » en bas à droite).
- v1.41.0 : forçage d'assignation d'un invité surprise sur une table pleine (migration `0049`, `p_force` ajouté à `assign_table_to_guest_approval_strict`, défaut false = comportement strict inchangé ; capacité cible non bloquante si forcé, destinations d'une réorganisation restent strictes, arrivés jamais déplacés, tracé dans `audit_logs`). Toggle « Forcer le placement » sur `/approbations/[id]/assign`. Correctif test `navigation-resilience.test.ts` (fenêtre `{0,200}` → `{0,400}`).
- v1.41.1/v1.41.2/v1.41.3 : alerte CodeQL ("DOM text reinterpreted as HTML", `js/xss-through-dom`) sur l'aperçu photo de `components/GuestApprovalCaptureFlow.tsx` (`preview`, alimenté par `URL.createObjectURL(photo)`, rendu comme `src` d'`<img>`). Garde-fou de type `safePreview` (préfixe `blob:` whitelist) ajouté en v1.41.1 — CodeQL ne le reconnaît pas comme sanitizer pour cette requête, l'alerte reste levée. Le commentaire de suppression inline documenté par GitHub (`codeql[js/xss-through-dom]`) tenté en v1.41.2 (deux placements) n'a lui non plus aucun effet : **ce repo utilise le Default setup CodeQL (aucun `.github/workflows/*codeql*` versionné)**, qui ne semble pas traiter ces commentaires — à vérifier si un jour le repo passe en Advanced setup. Alerte rejetée manuellement par Gersom comme faux positif dans l'onglet Security → Code scanning de GitHub (04/09/2026) ; le garde-fou `safePreview` reste en code (v1.41.3 retire le commentaire de suppression inefficace/trompeur, sans effet réel sur le comportement).
- v1.42.0 : lot de 8 correctifs/fonctionnalités (retour de Gersom, 4 photos, 13/09/2026). **Bug critique corrigé** : Approuver/Refuser ne faisait rien dans l'app ("La décision n'a pas pu être enregistrée") — la contrainte `guest_approval_requests_decided_via_check` en production n'autorisait jamais `'app'` (seulement `'web'`/`'whatsapp'`), alors que la décision app envoie toujours `'app'`. Root-cause trouvée par reproduction directe en base (transaction BEGIN/ROLLBACK). La migration `0037_guest_approval_app_push.sql` (déjà dans le dépôt depuis longtemps mais jamais appliquée en production — écart entre `supabase migrations list` et l'historique réel du dépôt) corrige exactement ça ; **appliquée en production le 13/09/2026**, avec au passage `push_subscriptions` (table manquante depuis toujours, notifications Web Push muettes sans erreur visible) et `0036_enable_rls_user_credential_backups.sql` (RLS manquante sur une table inutilisée, signalée par l'advisor Supabase). Aucune nouvelle migration écrite : ces deux fichiers existaient déjà tels quels. **Bottom nav** : Approbations remplace Agenda (barre générique admin/directeur hors `/dashboard`/`/scan`/`/agenda`) et Staff (visibilite) en dernière position. **Flash corrigé** dans `GuestArrivalPanel` (état `loading`/`members` de l'invitation précédente affiché brièvement en changeant d'invitation, ex: solo → groupe) : réinitialisé synchroniquement au changement de `invitation.id`. **`/checkin/[invitationId]`** : bloc "Table N" et étiquette `T0XX` cliquables vers `/tables/[tableId]`. **Caméra en direct** (`components/PhotoCaptureCamera.tsx`, `getUserMedia` + capture canvas, même mécanisme que `QrScanner.captureFrame`) remplace l'`<input capture>` qui ouvrait l'app Camera native pour "📷 Invité surprise". **"Arrivé(e) avec"** (`linked_invitation_id`) affiché dans `/approbations` (liste + détail) et sur `/approbations/[id]/assign`, où sa table est désormais **présélectionnée automatiquement** (priorité avant la table 41), modifiable en touchant une autre table. Fiche détaillée de `/approbations` resserrée (photo 42dvh → 26dvh).
- v1.42.1 : badge de version du splash déplacé (retour de Gersom : "c'est trop en bas à droite... une translation d'à peu près 1 cm à l'angle 315°") — `transform: translate(-0.71cm, -0.71cm)` (cap boussole 315° = nord-ouest) en plus de l'ancrage `bottom-3 right-4` existant, remonte le badge vers le centre au lieu de le laisser collé dans l'angle. La synchronisation automatique avec `package.json` (déjà en place depuis v1.40.0) est inchangée et reconfirmée.
- v1.43.0 : deuxième lot du 13/09/2026 (retour de Gersom, 5 photos). **Refus reconsidérable** : `applyGuestApprovalDecision` (lib/guestApprovalDecide.ts) accepte un 6e paramètre `allowReconsiderFromRefused` (vrai uniquement depuis `/api/guest-approvals/[id]/decide`, jamais sur `/approve/[token]` ni WhatsApp) qui autorise `refuse -> approuve` (jamais l'inverse) — bouton "Reconsidérer → Approuver" sur la liste et la fiche détaillée de `/approbations`. **Flash résiduel de `GuestArrivalPanel` corrigé** : la branche "ensure" (backfill d'une invitation jamais matérialisée, typiquement rencontrée depuis `/plan-table`) ne marquait pas `initializing=true` avant son fetch, contrairement à la branche "initialize" juste au-dessus — fenêtre où `settled=true` et `visible=false` faisait flasher l'ancien compteur. **Pincement cassé sur `/plan-table` après rotation** : le tire-pour-rafraîchir (Touch Events) et le zoom du plan (Pointer Events, `ZoomableFloorPlan.tsx`) vivaient sur le même conteneur et se disputaient les gestes à deux doigts — les deux gestionnaires de tire-pour-rafraîchir ignorent désormais `e.touches.length > 1`. **Swipe pour supprimer** (admin uniquement) une demande déjà décidée sur `/approbations` (`components/SwipeableDeleteCard.tsx` + `DELETE /api/guest-approvals/[id]`, refuse toute demande `en_attente`). **Texte de `/approbations/[id]/assign` raccourci** en un badge "Automatique"/"Sélectionnée". **agent_checkin** gagne `viewGuestApprovals` en lecture seule (jamais `reviewGuestApproval` ni `assignGuestApproval` — ce rôle continue de renvoyer vers un placeur) : Approbations remplace Agenda en dernier onglet de la barre du bas (4 emplacements seulement, pas de place pour les deux) ; l'agenda du jour reste visible sur `/scan` via `NextAgendaActivity`, déjà affiché pour ce rôle sans onglet dédié.
- v1.43.1 : `components/PushNotificationButton.tsx` affiche désormais des instructions précises par plateforme (iOS/Android détectés séparément) quand une action manuelle est nécessaire (refus explicite, ou iOS pas encore installé sur l'écran d'accueil), au lieu d'un message générique "à configurer" — retour de Gersom : "j'aimerais que ça m'amène directement dans les settings... un peu comme le prompt caméra". Aucune API web ne permet d'ouvrir directement les réglages système de notifications d'une PWA sur iOS ou Android (contrairement à `getUserMedia`/caméra, qui a sa propre invite native depuis la page) — restriction de plateforme documentée dans le code, les instructions pas-à-pas sont le meilleur substitut possible.
- v1.44.0 : reconsidérer un refus (v1.43.0) mène désormais d'abord au choix d'une table, avant l'approbation — retour de Gersom : "je n'avais pas l'option de le mettre sur une table... le process a été automatique". "Reconsidérer → choisir une table" (liste et fiche détaillée de `/approbations`) navigue vers `/approbations/[id]/assign`, qui gagne un troisième mode `reconsider` (statut `refuse`) en plus de `reserve`/`assign` : même écran de choix de table que la réservation avant-décision, mais valide via la nouvelle route `POST /api/guest-approvals/[id]/reconsider-assign` (exige `reviewGuestApproval` ET `assignGuestApproval`) qui réserve la table choisie puis approuve (`allowReconsiderFromRefused`) en une seule action — si la réservation échoue (table pleine entre-temps), rien n'est décidé. `reserve_table_for_guest_approval` (migration `0050_reserve_table_for_reconsidered_refusal.sql`, exécutée et vérifiée en production Supabase le 13/09/2026, `SECURITY INVOKER`) accepte désormais aussi le statut `refuse`, pas seulement `en_attente` — une demande refusée n'a jamais de `reserved_table_id` déjà posé (libéré automatiquement au refus), aucun risque de collision.
- v1.45.0 : deux correctifs (retour de Gersom, capture d'écran Agent001 sur `/scan`). (1) **Navigation `agent_checkin`** : "les agents scan sont là seulement pour scanner et non pour approuver — s'il y a un problème ils redirigent vers le placeur qui fait la photo. Le bouton au milieu devrait être tableau de bord, à droite agenda puis approbation." `AGENT_CHECKIN_ITEMS` (`components/BottomNav.tsx`) devient Recherche, Plan, **Tableau de bord** (`CENTRAL_HREF['agent_checkin'] = '/dashboard'`), Agenda, Approbations — plus d'onglet Scan dédié : `/scan` reste la page d'atterrissage par défaut de ce rôle (`landingPathForRole`) et reste joignable via la flèche Retour du TopBar (`/approbations` → `/scan` direct, `/agenda` → `/dashboard` → `/scan`). (2) **"Ça ne reste pas figé" pendant le défilement, bouton central injoignable ensuite — "make it really fixed like in an app"** : `hooks/usePullToRefresh.ts` (utilisé par `/dashboard`) gardait "seulement en haut de page" avec `window.scrollY <= 0`, structurellement toujours vrai dans cette app (page ancrée, seul un `<div overflow-y-auto>` interne défile) — le geste "tirer pour actualiser" pouvait donc se déclencher, et rester bloqué (aucun `touchcancel`), au milieu d'un défilement normal. Corrigé en vérifiant le `scrollTop` du vrai conteneur défilant (`containerRef`, comme `/plan-table` le faisait déjà) + `touchcancel` sur les deux. Chaque écran principal passe aussi de `h-dvh` seul à `fixed inset-0` (ancré au viewport réel plutôt que dépendant d'un recalcul de hauteur pendant qu'une barre d'outils système apparaît/disparaît en défilement) — aucun changement du flex interne, `BottomNav` garde exactement sa place.
- v1.45.1 : "les deux SS qui vont par-dessus le bouton recherche" (retour de Gersom, `/scan` tourné en paysage) — le bouton de compte flottant (`AccountMenu floating`, via `components/UserMenu.tsx`, utilisé uniquement sur `/scan` et `/placement`, les deux seuls écrans sans TopBar) restait ancré en haut à droite du viewport quelle que soit l'orientation, alors qu'en paysage la barre de navigation devient une bande verticale collée à ce même bord droit (`landscape:w-20`, `components/BottomNav.tsx`) : les deux se superposaient, recouvrant Recherche. Bascule désormais à gauche uniquement en paysage (`landscape:left-[calc(1rem+env(safe-area-inset-left))]`, portrait inchangé) ; son panneau déroulant s'ouvre vers la droite dans ce cas pour rester à l'écran. La bannière "Nouvelle approbation" du même composant, qui passait sous cette même bande en paysage, s'arrête désormais avant elle. L'usage non flottant (`TopBar`, toutes les autres pages) n'a jamais ce problème (déjà calé par le flux flex), aucun changement là.
- v1.45.2 : "à déréguler ce problème de app fixé même quand on met en horizontal" (retour de Gersom, `/plan-table` tourné en paysage sur iPad) — le dernier contenu visible (dernière rangée de tables) et le dernier onglet de la barre de navigation verticale pouvaient se retrouver recouverts par la barre gestuelle système, qui reste horizontale au bas de l'écran même en paysage sur iPad (contrairement à l'iPhone, où elle rejoint un des côtés). `.bottom-nav-glass` réserve déjà cet espace en portrait (`margin-bottom`), mais le remet explicitement à 0 en paysage (elle devient une bande verticale, protégée seulement côté `safe-right`) — rien ne protégeait le bord du bas, probablement masqué avant `fixed inset-0` (v1.45.0) par un calcul `h-dvh` qui excluait déjà cette zone sur certains navigateurs, exposé une fois le viewport réel utilisé directement. Corrigé en réduisant le rectangle de la coquille de chaque page en paysage (`landscape:bottom-[env(safe-area-inset-bottom)]` au lieu de `bottom-0`, sur les 11 pages utilisant `BottomNav`) : protège d'un coup le contenu ET le dernier onglet de la bande verticale, sans toucher au padding interne d'aucune page individuellement.
- v1.46.0 : la nav de `placeur` calque désormais le comportement contextuel de `directeur` — retour de Gersom sur Agent001 (comparé à Rémy Landu) : "je m'attends plutôt à voir un menu... un peu comme celui de Rémy Landu". Rôle vérifié directement en base avant de coder (`select role from users`) : Agent001 est bien `placeur`, pas `agent_checkin` comme les échanges précédents sur ce compte le laissaient penser — ce correctif ne touche en rien `agent_checkin` (déjà traité les 03 et 13/09/2026). Question posée à Gersom entre un simple remplacement du dernier onglet (Staff → Approbations, comme pour les autres rôles) et la reproduction complète du comportement de directeur ; il a choisi la seconde option. `components/BottomNav.tsx` : `isAdminDirector` devient `isDirectorStyleNav` (`role === 'admin' || role === 'directeur' || role === 'placeur'`) — Tableau de bord au centre hors `/dashboard`/`/scan`/`/agenda` (`CENTRAL_HREF.placeur = '/dashboard'`, `ITEMS.placeur` reprend exactement la forme de `ITEMS.directeur` : Recherche, Plan, Bord, Scan, Approbations, `STAFF_ITEMS` supprimée car devenue inutilisée), Scan/appareil photo au centre sur ces trois pages avec Agenda visible à côté. `lib/permissions.ts` : `placeur` gagne `viewAgenda` en lecture seule (jamais `manageAgenda`), nécessaire pour que ce nouvel onglet Agenda soit réellement accessible (même principe que le `viewAgenda` d'`agent_checkin`, v1.40.0) — `/staff` reste atteignable via le badge QR "STAFF" depuis `/scan` (`viewStaff` inchangée).
- v1.46.1 : `agent_checkin` (Scotty Sanda) rejoint à son tour `isDirectorStyleNav`, même jour — retour de Gersom : "quand on est dans le tableau de bord, je voudrais que le bouton doré en bas soit le bouton scan plutôt". Le bouton central de ce rôle (Bord, `CENTRAL_HREF.agent_checkin`) pointait vers `/dashboard` même en y étant déjà, un aller-retour inutile déjà corrigé pour admin/directeur le 02/09/2026. Scan (jamais l'appareil photo, ce rôle n'a pas `submitGuestApproval`) devient le centre sur `/dashboard`/`/scan`/`/agenda`, Bord ailleurs — `AGENT_CHECKIN_ITEMS` (barre générique) inchangé.
- v1.47.0 : mise à jour ciblée de la liste d'invités depuis `guest-list_48.csv` (export With Joy le plus récent transmis par Gersom le 14/09/2026, "tu as tous les droits... on ne change pas toute la base, juste quelques changements") + nouvelle table 42. **Table 41 renommée « Houston » et devenue régulière ; nouvelle table 42 « Johannesburg » devient l'unique réserve** (capacité 10, confirmée par Gersom), migration `0051_table_42_johannesburg_reserve.sql` appliquée et vérifiée en production le jour même — nouvelle structure : 42 tables, 410 places officielles, 420 places absolues (`lib/withjoyImport.ts`, `app/plan-table/page.tsx`, `app/admin/import-withjoy/page.tsx`, `components/CapacityGauge.tsx` et toute la documentation versionnée mis à jour en conséquence). **Comparaison nom-par-nom** (jamais un réimport complet/destructif via `/admin/import-withjoy`, conformément à `docs/DATA_CHANGE_INSTRUCTIONS.md` section 6) entre les 252 invitations existantes et les 407 lignes du CSV, en réutilisant `lib/withjoyImport.ts` (`parseCsvText`/groupement par `party`) : **201 rafraîchissements de contact** (téléphone/email) sur des invitations déjà présentes reconnues par correspondance exacte de nom ; **19 nouvelles invitations** sans correspondance ajoutées avec leurs coordonnées, **volontairement sans table assignée** — le plan de table photographié transmis par Gersom (nouvelle configuration) montre des occupants différents de ceux actuellement en base pour plusieurs tables cibles (ex. table 29 : "Famille Vincent Nkouka" + "Tia Nzuzi Culumbu" en base, "Famille Nzasi"/"Famille Manuel" sur la photo) — une réorganisation complète des places déjà occupées était hors périmètre explicite de cette demande ; la table lue sur la photo est conservée dans les notes de chaque nouvelle invitation pour référence, jamais devinée silencieusement pour un placement effectif. **Bergine Momene et Ralph Momene** (déjà réunis dans "Famille Momene" en base) et **Tantine Angèle Lukau** (nouvelle invitation) marqués `ne_viendra_pas = true` sur confirmation explicite de Gersom ("they will not come to wedding, we will only send them after probable"). **Bug réel trouvé pendant cette revue** : `app/approbations/[id]/assign/page.tsx` codait en dur la priorité de placement sur `usage.table.number === 41` (jusqu'ici la seule réserve) — corrigé en `=== 42` puisque 41 est désormais une table régulière, sans quoi "Houston" aurait été proposée en priorité devant la vraie réserve. **Non fait dans ce lot** (explicitement hors périmètre, ou bloqué sur la fiabilité de l'OCR d'un plan photographié plutôt qu'un export machine-lisible) : redessiner `components/FloorPlan.tsx` selon la nouvelle configuration de zones (20 tables sud, 22 tables nord en 5 rangées de 4 + 2 tables ouest) — la table 42 n'apparaît donc pas encore sur ce plan visuel optionnel, bien que pleinement fonctionnelle partout ailleurs ; surbrillance des chaises par personne (explicitement laissée à discrétion par Gersom) ; réassignation des invitations déjà en base vers une nouvelle table selon la photo.
- v1.48.0 : suite directe de v1.47.0, sur demande explicite de Gersom ("Finish floor-plan visual redesign... Estimate using database to help") — feu vert donné pour utiliser l'OCR des photos malgré le risque d'erreur, en l'atténuant par recoupement avec la base plutôt qu'en attendant un export machine-lisible qui n'est jamais arrivé. **`components/FloorPlan.tsx` entièrement redessiné en deux zones (nord/sud)** reproduisant l'ordre/voisinage exact des deux photos (OCR table par table, 44 crops relus un par un, méthodique) : zone nord 5×4, zone sud 6×4 (2 cases vides = "Piste et File Attente" de la photo, déjà couverte par la salle "Piste de danse" existante). Les 42 tables ont désormais toutes une position, y compris la 42 ("Johannesburg"). **Bug réel trouvé en recroisant avec la base** : la rangée 4 de la zone nord avait les tables 10/16/17/9 sur les mauvaises colonnes (`1,10,16,17,9` au lieu de `1,9,10,16,17`) — corrigé après re-vérification. **`lib/floorPlanSeats.ts`** (nouveau) : noms de sièges par table lus sur les photos, recoupés avec les 272 invitations actuelles (nom exact + tolérance approchée) — ~87% des noms trouvent une correspondance (mêmes noms déjà ajoutés sans table en v1.47.0 pour 19 d'entre eux, confirmant que les deux extractions indépendantes — tags CSV et OCR photo — concordent), et ce recoupement a permis de corriger plusieurs lectures OCR incertaines (ex. "Jean-Ciben Ca ous" → "Jean-Clivens Le Caous", confirmé en base). **PUREMENT INFORMATIF** : jamais écrit en base, jamais une source de placement (qui reste `invitations.table_id`) — affiché comme panneau optionnel "Vu sur le plan photographié" sous la fiche de table sélectionnée sur `/plan-table`, avec sièges togglables en surbrillance (état local uniquement). Les 15 écarts identifiés entre le plan photographié et les tables actuelles en base (ex. "Furty Matuba" photo table 18 vs base table 37) ne sont volontairement PAS appliqués dans ce lot — resteraient à traiter séparément si Gersom le demande explicitement (réorganisation des places déjà occupées, hors périmètre confirmé en v1.47.0).
- v1.53.7 : retour de Gersom (capture d'écran de Messages iOS) — « No, I really want the swipe... just like an iPhone » : le bouton de suppression explicite de v1.53.6 n'était qu'un filet de sécurité, pas ce qu'il demandait. `components/SwipeableDeleteCard.tsx` entièrement reconstruit : abandon complet des Pointer Events (deux échecs consécutifs confirmés sur son appareil réel, v1.53.2 et v1.53.4, malgré des approches opposées — le choix d'approche lui-même était en cause, pas un détail) au profit d'un **vrai défilement horizontal natif** (`overflow-x-auto` + `scroll-snap-type: x mandatory`, deux panneaux contenu/bouton "Supprimer") : le navigateur gère seul le geste et sa cohabitation avec le défilement vertical de la liste, exactement le mécanisme qui alimente le glissement natif de Messages iOS. `.no-scrollbar` (`app/globals.css`) masque la barre de défilement du conteneur. Le bouton explicite de v1.53.6 reste en place en parallèle comme filet de sécurité. Diagnostic par lecture du code/de la spécification, aucun appareil iOS réel disponible dans cet environnement — à reconfirmer par Gersom.
- v1.53.6 : retour de Gersom (2 captures d'écran) — « le problème de la notification, ça fonctionne, c'est bon » (v1.53.4 confirmé fonctionnel) mais « swipe to delete, ça ne fonctionne pas... comment les delete vraiment, les enlever du système ? ». Après deux corrections différentes du geste tactile (v1.53.2 verrouillage d'axe, v1.53.4 capture immédiate du pointeur), toutes deux confirmées totalement silencieuses sur son iPhone réel, et sans aucun appareil iOS disponible dans cet environnement pour déboguer une troisième fois à l'aveugle : **bouton de suppression explicite (icône poubelle)** ajouté sur chaque carte de `/approbations`, admin uniquement, réservé aux demandes déjà décidées — garanti cliquable indépendamment de tout comportement tactile propre à la plateforme, protégé par une confirmation (`window.confirm`, action irréversible). Le geste de glissement (`SwipeableDeleteCard`) reste en place en parallèle au cas où il finirait par fonctionner sur d'autres appareils ; la garde serveur (`DELETE /api/guest-approvals/[id]` refuse toute demande encore `en_attente`) protège les deux chemins de façon identique. Aucune migration.
- v1.53.5 : retour de Gersom (capture d'écran de la fiche détaillée d'une demande "Test") — « il y a un problème avec les deux flèches, corrige » : la flèche précédente recouvrait partiellement le badge "Côté Gégé/Nelly" juste sous la photo, texte illisible. Root cause dans `app/approbations/page.tsx` : les boutons précédente/suivante étaient positionnés à un pourcentage fixe de la hauteur du **viewport** (`fixed left-3/right-3 top-[46%]`), une estimation censée les aligner sur la photo mais qui ne correspondait pas à sa position réelle une fois la hauteur variable de l'en-tête (titre + bouton fermer) prise en compte. Corrigé en ancrant les deux boutons au conteneur de la photo lui-même (nouveau wrapper `relative` autour de l'`<img>`/du placeholder), position et hauteur toujours connues — les flèches restent désormais systématiquement centrées sur la photo, jamais sur le contenu en dessous, quel que soit l'écran ou la longueur du titre. Tailles responsives conservées (44px mobile, 56px à partir de `sm:`, flottant hors de la fiche sur grand écran). Aucune migration.
- v1.53.4 : retour de Gersom (capture d'écran `/approbations` + message vocal) — (1) « je vais encore à configurer » alors qu'une vraie alerte push venait d'arriver sur son iPhone (notifications réellement actives) ; (2) « je ne peux pas swipe left or right » — le swipe pour supprimer (correctif v1.53.2) toujours totalement non fonctionnel à un second test réel, alors que le tap pour ouvrir la carte marche. **(1)** `components/PushNotificationButton.tsx` n'avait aucun `useEffect` réconciliant son état local avec la permission/l'abonnement réels au chargement — le statut démarrait toujours à `'idle'` et ne changeait qu'en réaction à un clic dans la session en cours ; un échec ponctuel lors d'un essai antérieur laissait le message bloqué sur « à configurer » même une fois l'abonnement effectivement enregistré et fonctionnel. Nouveau `useEffect` au montage : `Notification.permission === 'granted'` + un `pushManager.getSubscription()` existant ⇒ statut `'enabled'` immédiatement ; `Notification.permission === 'denied'` ⇒ statut `'denied'` immédiatement — sans attendre un nouveau clic. **(2)** Le « verrouillage d'axe » de v1.53.2 (ne capturer le pointeur qu'après avoir détecté un geste horizontal) est confirmé insuffisant : sur iOS Safari, ne pas capturer immédiatement à `onPointerDown` laisse apparemment le moteur natif trancher seul le sens du geste, et avec `touch-action: pan-y` il tranche presque toujours pour un défilement vertical natif avant que React ne reçoive de `pointermove` horizontal exploitable — zéro réaction, même partielle. Nouvelle approche dans `components/SwipeableDeleteCard.tsx`, conforme à la spécification touch-action/Pointer Events (celle des bibliothèques de glissement usuelles) : capturer le pointeur **immédiatement** à `onPointerDown`, suivre `pointermove` pour tout déplacement horizontal, et laisser le navigateur annuler proprement la séquence (`pointercancel`, déjà géré par `onPointerUp`) dès qu'il bascule lui-même vers un défilement vertical natif (`touch-action: pan-y` continue de le permettre malgré la capture). Diagnostic par lecture du code/de la spécification (aucun appareil iOS réel disponible dans cet environnement) — à reconfirmer par Gersom. Aucune migration.
- v1.53.3 : retour de Gersom (2 captures d'écran, `/tables/[tableId]` table 1 et fiche "Famille Mbiyavanga Mavinga"/table 4). (1) **Badge de statut compact** : `StatusBadge` gagne une variante `compact` (point coloré 10px, sans texte — libellé conservé via `title`/`aria-label`) utilisée uniquement sur `/tables/[tableId]`/`/table/[tableId]` (ligne cumulant déjà nom + compteur + jusqu'à quatre icônes) ; `/search`/`/staff`/`/dashboard/liste` gardent le badge texte complet, chacun ayant sa propre ligne. (2) **Toucher une ligne met en évidence son siège** sur ces deux mêmes routes (au lieu de naviguer vers `/checkin/[invitationId]`, redondant depuis le bouton ✅ dédié de v1.51.0) quand une correspondance de siège existe ; sinon la ligne continue de naviguer. Le bouton 🪑 réutilise la même fonction `highlightSeats`. (3) **Dernier assessment des 393 sièges de `lib/floorPlanSeats.ts`**, recoupés table par table avec les 250 invitations actuelles : 75 corrections OCR confiantes appliquées (« Fixon Zola » → « Fiston Zola » table 4 ; « Jean-Ciben Ca ous » → « Jean-Clivens Le Caous » table 1, déjà identifiée en v1.48.0 mais jamais reportée ; « Edo Tukwa » → « Edo Tukula » table 3 ; 72 autres, liste complète en tête du fichier). 13 cas ambigus (plusieurs candidats trop proches sur la même table, ex. table 4 Momba/Mavinga) et 5 sans candidat volontairement PAS devinés, signalés à Gersom (voir CHANGELOG v1.53.3) — dont deux divergences déjà connues (Bembo/Mateus table 40, Diego Ramos/Jade Magnus table 41) datant d'une réorganisation postérieure à la photo (v1.50.0), pas des erreurs de lecture. Aucune migration.
- v1.53.2 : retour de Gersom (2 captures d'écran) — (1) « Demande d'approbation » affichant en rouge le message d'erreur Twilio alors que Twilio est intentionnellement désactivé (« assure-toi qu'on n'a pas ce message... si c'est désactivé, tous ces problèmes-là disparaissent... mets un bouton dans le paramètre de la page admin pour activer/désactiver ») ; (2) « je n'ai toujours pas la possibilité de swipe » sur `/approbations`. **(1) Bouton bascule « SMS/WhatsApp (Twilio) » sur `/admin`** : remplace l'interrupteur par variable d'environnement `TWILIO_ENABLED` (v1.48.3, nécessitait un accès Vercel) par `events.twilio_enabled` (migration `0055_events_twilio_enabled.sql`, exécutée et vérifiée en production Supabase le jour même), modifiable en un clic depuis l'app — `lib/twilio.ts`/`lib/guestApprovalNotify.ts` reçoivent désormais cet état en paramètre explicite (lu depuis `events` par chaque appelant) plutôt que de lire une variable d'environnement eux-mêmes. `POST /api/guest-approvals` distingue désormais `sms_skipped` (désactivé par le toggle — jamais un problème) de `sms_error` (activé mais mal configuré, ou refus d'envoi — un vrai problème) ; `GuestApprovalCaptureFlow.tsx` n'affiche plus le bandeau rouge quand `sms_skipped` est vrai. **(2) Swipe pour supprimer corrigé** : rôle admin du compte concerné vérifié directement en base avant d'aller plus loin (`nom_affichage = "Admin"`, `role = "admin"`, confirmant que l'initiale "AD" affichée ne trahissait aucun souci de permission). Root cause dans `components/SwipeableDeleteCard.tsx` : le pointeur était capturé (`setPointerCapture`) dès `onPointerDown`, avant même de savoir si le geste serait horizontal ou vertical — sur WebKit/iOS, ceci combiné à `touch-action: pan-y` peut faire annuler la séquence de pointeur dès la moindre composante verticale, empêchant toute détection du swipe. Corrigé par un verrouillage d'axe (`AXIS_LOCK_THRESHOLD`) : le pointeur n'est capturé qu'une fois le mouvement assez net pour trancher horizontal (poubelle) vs vertical (défilement natif intact, jamais de conflit). Diagnostic par lecture du code (aucun appareil iOS réel disponible dans cet environnement pour reproduire) — à reconfirmer par Gersom.
- v1.53.1 : bug réel signalé par Gersom (capture d'écran `/scan`, thème Maison) — le titre de la page ("Scanner un QR code") apparaissait superposé et illisible avec la bannière transitoire "Nouvelle approbation" de `components/AccountMenu.tsx`, les deux textes se lisant l'un à travers l'autre. **Root cause** : cette bannière (`fixed` par-dessus le contenu) utilisait `bg-surface` nu + `shadow-elev-2`, sans le `backdrop-filter` (flou + saturation) que toutes les autres surfaces « verre liquide » de l'app appliquent systématiquement avec cette même couleur — `--surface` vaut une quasi-transparence en Maison (alpha 0.045), pensée pour être vue à travers un flou, jamais seule à plat ; sans lui, le contenu de la page en dessous transparaissait intégralement à travers la bannière (`/scan` est le seul écran dont le titre commence immédiatement sous elle, sans barre de navigation opaque entre les deux). Nouvelle classe `.glass-toast` (`app/globals.css`, même recette que `.card` : `box-shadow: var(--elev-2)` + `backdrop-filter: blur(20px) saturate(160%)`) appliquée à la bannière ; comportement et positionnement (paysage compris) inchangés. Aucune migration.
- v1.53.0 : retour de Gersom (15/09/2026, en réponse à une question posée sur l'alignement des colonnes de `/admin/import` avec le format CSV With Joy, en s'appuyant sur `Withjoy_template_csv.csv` transmis en référence) — revirement explicite en cours de message : « Ah non, tu sais quoi ? Mauvaise idée. Enlève cette fonction. On va seulement garder les imports à partir du CSV de Witjoy pour simplifier les éléments. » **Trois écrans admin retirés en même temps** (« donc tout ça ça va être des... toggle off »), `app/admin/page.tsx` ne garde que « Importer depuis With Joy » comme parcours d'import : (1) **`/admin/import`** (import CSV/XLSX générique par association manuelle de colonnes, `app/api/admin/import/route.ts`) — supprimé ; `/admin/wizard` (étapes 5 « Importer les invités » et 6 « Associer chaque invitation à une table », jusqu'ici les deux seules à pointer vers cet écran) redirige désormais vers `/admin/import-withjoy`, seul chemin d'import restant. (2) **« Diffuser les invitations »** (`/admin/diffusion`, `lib/invitationDiffusion.ts` — traitement Excel/CSV entièrement local au navigateur, jamais une écriture serveur) — supprimé, `tests/invitation-diffusion.test.ts`/`npm run test:diffusion` retirés avec. (3) **« Associer les QR codes »** (`/admin/qr`, `app/api/admin/qr/route.ts` — écran admin de création/association `code`↔`table_id` dans `qr_codes`) — supprimé ; **la table `qr_codes` et sa lecture en scan restent intactes** (`app/scan/page.tsx`, `app/placement/page.tsx` continuent de lire cette table pour reconnaître un QR physique déjà posé) — seul l'écran de gestion disparaît, toute nouvelle association passera directement par Supabase. `/admin/wizard` perd son étape 7 dédiée au QR (`stats.qrTotal`/`qrManquants` retirés de `/api/admin/wizard-status`) ; l'assistant passe de 10 à 9 étapes, renumérotées. « Gérer les tables » (`/admin/tables`) confirmé inchangé par Gersom. Aucune migration : uniquement des écrans/routes retirés et des liens internes recâblés, aucune donnée touchée.
- v1.52.0 : retour de Gersom (15/09/2026) : tentative d'import complet depuis `/admin/import-withjoy` avec `guest-list_56.csv` ("le CSV final... écrase ce qui est dans l'app"), bloquée par "Échec atomique de l'import". **Bug réel #1** : `admin_replace_invitations` (`delete from invitations`) échouait des qu'une `guest_approval_requests.linked_invitation_id` pointait vers une invitation supprimée — cette FK (migration 0046, 02/09/2026) n'avait jamais reçu de clause `ON DELETE` (contrairement à `audit_logs`/`exceptions`, deja `SET NULL`), oubli jamais declenche faute d'un vrai remplacement complet tente avant. Corrige par la migration `0054_guest_approval_linked_invitation_on_delete_set_null.sql` (`ON DELETE SET NULL`, meme comportement que les autres references). **Bug réel #2** : `lib/withjoyImport.ts` ne retirait jamais une apostrophe de tableur parfois presente devant le numero de telephone dans la colonne "phone number" du CSV (ex. `'+41799150386`, convention de tableur pour forcer le format texte) — corrige (`cleanPhone`), `scripts/build_plan_from_csv.py` garde synchronise, `tests/withjoy-import.test.ts` (2 nouveaux tests). Une fois ces deux bugs corriges, **remplacement complet execute** via `admin_replace_invitations` (263 → 259 invitations, sauvegarde automatique `import_backups`) : decision explicite d'ecraser plutot que de continuer la synchronisation ciblee des versions precedentes (v1.47.0-v1.51.0), puisque Gersom a confirme que plusieurs elements en base ne correspondaient plus au CSV a jour. **0 table en surcapacite verifie apres ce remplacement** -- confirme que "les tables en surcapacite" signalees precedemment sont bien resolues par ce CSV final. `nombre_arrive = 0` confirme partout avant execution (`event.status = 'test'`).
- v1.51.0 : retour de Gersom (2 photos : un agent scan renommant un invité, la fiche de la table 1), migration `0053_rename_member_syncs_solo_display_name.sql` appliquée et vérifiée en production le jour même. (1) **Renommage réservé** : `agent_checkin` avait `manageMembers` depuis toujours (renommer une invitation entière ou un membre depuis « Qui est arrivé ? ») — retiré ("l'agent qui scanne ne doit pas pouvoir cliquer sur le nom et modifier... seul placeur, directeur de festin ou admin peut faire ça"), `lib/permissions.ts` seul point de verite, `/api/invitations/rename` et `/api/members/rename` ajoutees au filet `canAccessPath` pour ce role par coherence (l'API refusait deja cote serveur). (2) **Le nom du haut suit la correction pour une invitation solo** : `rename_invitation_member` (migration 0053) met desormais aussi a jour `invitations.nom_affichage` quand l'invitation n'a qu'un seul membre (compte via `invitation_guests`) — jamais pour un groupe, dont le libelle ("Famille X") reste distinct de chacun de ses membres. La propagation temps reel entre agents (table `guests` deja dans `supabase_realtime` depuis l'origine) fonctionnait deja, verifiee sans correctif necessaire. (3) **Trois icones par invitation sur `/tables/[tableId]` et `/table/[tableId]`** (retour de Gersom : "je ne suis pas capable d'aller dans la prochaine page pour les invitations facilement... comment je quitte ce mode ?") : le bouton unique 📍 (v1.48.8/v1.48.9) devient 📍 (localise la table dans la salle, navigue vers `/plan-table?table=N` qui se deplie directement sur le plan visuel et met cette table en evidence — reutilise `locateOnPlan`, nouveau parametre d'URL lu via `useSearchParams`, `/plan-table` gagne un boundary `Suspense`), 🪑 (reprend exactement l'ancien comportement de 📍 : met le siege en evidence sur le dessin "vu sur le plan photographie"), et ✅ (ouvre explicitement `/checkin/[invitationId]`, en plus du tap sur le nom, jamais un remplacement).
- v1.50.0 : suite directe de v1.49.0, sur demande explicite de Gersom (« tables en surcapacité corrected, jade magnus n'est plus invité, voici le dernier a jours de with joy » — `guest-list_53.csv` puis `guest-list_54.csv` puis `guest-list_55.csv`, ce dernier ajoutant Tio Godart et son épouse table 40 confirmé par une capture d'écran du plan seatplan.io de cette table). Aucune migration. `nombre_arrive = 0` confirmé sur les 29 invitations touchées avant écriture. **Jade Magnus supprimée** (n'est plus invitée). **7 données de test supprimées** ("Gtfxfyh test", "Hsbsbshshs", "Gersom Test", "Lui Dos", "Tetsfffd", "X", "Testt" — fiches "Invité surprise approuvé" créées pendant les tests du parcours d'approbation, occupant à tort des places aux tables 1/41/42 et faussant les calculs de capacité depuis plusieurs versions ; la table 41 Houston s'en retrouve entièrement vidée). **5 doublons supplémentaires** trouvés par le même recoupement téléphone/party-id/absence des exports récents qu'en v1.49.0 : "Famille Neves" scindée en deux vraies invitations (Andrea reste table 11, Ketsia devient table 1 — sa propre fiche placeholder v1.47.0 avait déjà le bon téléphone) ; "Famille Manuel" (doublon de "Famille non-nommé"/Sergio Manuel, table 29) ; "Famille Mvovi" fusionnée dans Costa Mvovi (même party-id With Joy) ; "Tia Nzuzi Culumbu" standalone fusionnée dans "Famille Nzuzi Culumbu" (désormais table 8) ; "Daniel Victor" (disparu des exports With Joy depuis guest-list_50, même téléphone qu'"Oredezo Blancky") renommé. **Table 40 scindée** sur confirmation croisée tag CSV + photo du plan seatplan.io : "Famille Matondo" (Eude+Francisco restent table 40, Huguette+Julianna table 36) et "Famille Lembe" (Youyou seule table 31, Odon Tchiteya sans tag ni photo — fiche séparée sans table, signalé). **Table 41 débloquée** une fois vidée des données de test : Famille Bembo (3) + Famille Menga (4) + Luzolo Patrick Menga (1) = 8/10, résolvant le blocage v1.49.0. Maria Irène Gomes placée table 18 (Famille Momene, déjà `ne_viendra_pas=true` depuis v1.47.0, ne comptait pas vraiment dans la capacité). **Non résolu, signalé à Gersom** : "Famille Michaud" reste sans table (le tag CSV T041 remplirait exactement les 2 dernières places de la table 41, mais la photo du plan de la table 40 les y montre assis — conflit réel, décision explicite demandée) ; table 29 toujours bloquée (1 seule place libre après nettoyage des doublons, Famille Vincent Nkouka sans tag CSV) pour Famille Nzasi (3) et Laura Humba (1) ; table 34 toujours pleine (10/10) pour Jean-Claude Nsenda ; "Odon Tchiteya" sans table ni tag ; les 21 invitations déjà placées à tag CSV divergent (dont Famille Matuba 37→18) restent hors périmètre.
- v1.49.0 : mise à jour ciblée depuis `guest-list_52.csv` (nouvel export With Joy transmis par Gersom, "c'est synchronisé entre seatplan et joy" — 3 photos + PDF `seating-chart-Mariage-Nelly---Gege-2026-09-14.pdf`, 42 tables du plan seatplan.io). Comparaison nom-par-nom + **numéro de téléphone** (jamais un réimport complet) entre 274 invitations et 406 lignes CSV. Aucune migration. Aucune arrivée enregistrée (`nombre_arrive = 0` partout, `event.status = 'test'`) au moment des écritures. **Découverte méthodologique importante** : croiser par téléphone (pas seulement par nom) les 6 fiches "ajoutées sans table" de v1.47.0 avec le reste de la base a révélé que 5 des 6 étaient en réalité déjà présentes ailleurs sous un nom légèrement différent (doublons créés par l'import v1.47.0, qui ne les avait pas reconnues comme les mêmes personnes) : Nzuzi/Domingos (même téléphone, noms fabriqués des deux côtés — vrais noms "Lucie Nzuzi, Seba Domingos" restaurés), Nzuzi Laisana/Denzu Laisana (faute de frappe With Joy corrigée dans le nouvel export), Ndani Ndoba/Famille Ndani (nom de famille tronqué), Dos Goncalves/Famille Mbala Dos Goncalves (même téléphone ET email), Bembo/"Famille suisee cousine" (nom fabriqué à partir d'une description "cousine suisse" prise pour un patronyme). Les 5 doublons supprimés, les fiches restantes corrigées avec les vrais noms. Seule "Famille Lusuena" s'est confirmée sans doublon et complétée avec table (12). 26 rafraîchissements de contact ; 2 nouvelles invitations sans table (Maria Irène Gomes, Jean-Claude Nsenda). **Non fait, signalé à Gersom** : 5 tables en surcapacité si les tags CSV sont appliqués tels quels par-dessus les occupants actuels (1, 9, 18, 29, 34, 41) — nécessiterait de déplacer des gens déjà placés ailleurs, hors périmètre de ce lot ciblé ; 2 fiches dont les membres ont des tags de table contradictoires entre eux (Famille Matondo T36/T40, Famille Neves T1/T11) ; 21 invitations déjà placées dont le tag CSV diffère de la table actuelle (dont "Famille Matuba" 37→18, discrépance déjà documentée en v1.47.0/v1.48.0) ; "Jade Magnus" disparue du CSV sans marque de refus ; l'échange physique confirmé des tables 18/34 sur le plan de salle (`components/FloorPlan.tsx`, positions visuelles uniquement) ; le rafraîchissement complet de `lib/floorPlanSeats.ts` depuis le nouveau plan seatplan.io (42 tables, désormais confirmé synchronisé avec With Joy).
- v1.48.9 : sens inverse (siège → invitation/membre) du dessin "vu sur le plan photographié", sur les trois écrans qui l'affichent, aucune migration. Retour de Gersom (3 photos) : "si j'appuie sur un des noms de la table, ça me surligne en haut quelle invitation ça fait partie" (`/tables/[tableId]`, `/table/[tableId]`) et "si j'appuie sur la chaise de la personne en dessous, ça me surligne directement dans cette page parmi les invités... c'est qui" (`GuestArrivalPanel`) — le sens nom→siège existait déjà (v1.48.5/v1.48.8), pas l'inverse. `lib/floorPlanSeats.ts` gagne `namesMatch(a, b)` (factorise la comparaison exacte déjà utilisée par `findSeatIndexByName`) ; les trois `onSelectSeat` retrouvent désormais, parmi les invitations/membres déjà listés, celle/celui dont le nom correspond exactement au siège touché, et surlignent sa ligne + défilent vers elle. **Signalé à Gersom plutôt que tenté silencieusement** : "quand j'ajoute quelqu'un dans l'application, ça l'ajoute aussi sur la table [dessin]" est incompatible avec la nature actuelle du dessin (lecture OCR statique de deux photos figées, `TABLE_SEAT_NAMES`, jamais une structure vivante) — nécessiterait la table `sièges` à ID stable déjà mise en attente en v1.48.6. La demande plus large ("dès qu'on te donne un CSV With Joy, mets à jour invitations/tables/sièges dans Supabase") reste elle aussi bloquée : toujours aucun identifiant stable par personne dans les exports reçus (v1.48.6), et la décision sur le nœud Nzuzi/Domingos/Culumbu (tables 6/29/32, `guest-list_51.csv`) reste en attente de réponse. `tests/table-seat-wheel.test.ts`/`tests/floor-plan-seats.test.ts` mis à jour.
- v1.48.8 : deux ajouts au dessin "vu sur le plan photographié" (4 photos, retour de Gersom), aucune migration. (1) **`/plan-table`** : "quand j'appuie sur Jonas, j'aimerais aussi que son nom en haut dans la fiche soit surligné" — toucher une invitation dans la liste au-dessus du dessin surligne désormais aussi sa ligne (fond teinté + liseré accent), pas seulement son ou ses siège(s) sur le dessin en dessous. Nouvel état `selectedInvitationId` dans `app/plan-table/page.tsx`, réinitialisé partout où `highlightedSeats` l'est déjà. (2) **`/tables/[tableId]` et `/table/[tableId]`** (fiche d'une table, jusqu'ici sans aucun dessin de plan) : "en dessous des noms, on puisse aussi afficher la table... garder la même logique... savoir où est-ce que la personne est assise" — les deux routes (nouvelle et historique, toujours maintenues en parallèle) gagnent le même panneau "Vu sur le plan photographié" que `/plan-table` et `/checkin/[invitationId]`, affiché sous la liste. Bouton 📍 séparé par invitation (uniquement quand une correspondance de nom exacte existe) : n'entre pas en conflit avec le tap sur le nom, qui continue d'ouvrir `/checkin/[invitationId]` comme avant — contrairement à `/plan-table`, où `onSelectInvitation` remplace déjà la navigation. Même mécanisme réutilisé partout (`lib/floorPlanSeats.ts`, `components/TableSeatWheel.tsx`), toujours purement informatif, jamais une source de placement. `tests/table-seat-wheel.test.ts` (3 nouveaux tests).
- v1.48.7 : nettoyage interne de `components/BottomNav.tsx`, aucun changement de comportement — retour de Gersom après un point d'étape "vers la 2.0" (ce qu'il faudrait nettoyer plutôt que d'empiler encore des patches) : "BottomNav a maintenant plusieurs cas spéciaux par rôle qui pourraient se resimplifier une fois la nav stabilisée." `ITEMS.admin`/`ITEMS.directeur`/`ITEMS.placeur` étaient trois littéraux (quasi-)identiques (seul l'ordre interne d'`admin` différait, sans effet réel car la répartition gauche/centre/droite se base toujours sur `SIDE_ORDER`/`CENTRAL_HREF`, jamais sur cet ordre de tableau) — remplacés par une constante partagée `DIRECTOR_STYLE_ITEMS`. Les trois branches contextuelles `isDirectorStyleNav` (`/dashboard`, `/agenda`, `/scan`) n'ont volontairement **pas** été fusionnées malgré leur ressemblance (`/agenda` et `/scan` identiques, `/dashboard` différent) : une dizaine de tests existants épinglent précisément leur structure source séparée, fusionner aurait exigé une réécriture disproportionnée au gain. Tests mis à jour en conséquence (`tests/approbations-ux-improvements.test.ts`, `tests/navigation-resilience.test.ts`, `tests/placeur-director-nav.test.ts`), aucune migration.
- v1.48.6 : "ajoute à la logique de seats [un] id... assure-toi que chaque siège a son id et fait match avec l'invité id... car après ça on va te donner un With Joy directement dans l'app [qui] doit placer les gens" — préparation pour un futur import final With Joy, sans encore construire cet import (demande explicite de Gersom : "analyse les csv... je crois ils ont un id qui ressemble à INV001" puis "attendre le fichier With Joy" avant de bâtir l'infrastructure complète). **Analyse de `guest-list_50.csv`** (transmis pour se faire une idée) : la colonne `party` de With Joy (ex. `table-002-party-006`) est bien un identifiant stable pour la même personne/le même groupe entre deux exports différents (vérifié : identique sur `guest-list_48.csv` et `guest-list_50.csv` pour Pajos Mpapa) — contrairement au nom. **Mais découverte importante, avant d'agir dessus** : le numéro qu'elle contient parfois (`table-XXX`) NE correspond PAS à notre vraie table — vérifié sur les 39 groupes multi-membres de `guest-list_50.csv`, seulement environ un tiers concordent avec le vrai tag `F0xx`/`T0xx` de la colonne `tags` (ex. Pajos Mpapa : `table-002-party-006` dans `party`, mais réellement `F006` = table 6). C'est un identifiant interne à une fonctionnalité de placement PROPRE à With Joy, distincte de celle qui produit nos tags `F0xx`/`T0xx` — jamais une source de placement pour notre app. **`invitations.withjoy_party_id`** (migration `0052_invitations_withjoy_party_id.sql`, colonne texte nullable, exécutée et vérifiée en production Supabase le 14/09/2026) stocke donc la valeur BRUTE de `party`, uniquement pour retrouver la même invitation lors d'un futur import (jamais parsée/utilisée pour le placement). `lib/withjoyImport.ts` : `ImportGroup` gagne `withjoyPartyId` (thread la valeur brute de `party`, `null` pour un groupe `SOLO-N` synthétique quand la colonne est vide dans le CSV) ; `admin_replace_invitations` (redéfinie dans la même migration, même corps que 0026 sauf la colonne ajoutée) et `app/api/admin/import-withjoy/route.ts` écrivent désormais ce champ lors d'un import complet. Le placement continue exclusivement de venir du tag `F0xx`/`T0xx`, inchangé. **Non fait dans ce lot** (explicitement remis à plus tard, sur demande de Gersom) : table `sièges` avec ID stable par chaise, construction de l'import final lui-même — à faire une fois le fichier With Joy définitif transmis. `tests/withjoy-import.test.ts` (4 nouveaux tests).
- v1.48.5 : retour de Gersom (3 photos, `/plan-table` et `/checkin/[invitationId]`) — deux demandes liées au dessin "vu sur le plan photographié". (1) **Le dessin apparaît désormais aussi sur la fiche d'un invité** (`/checkin/[invitationId]`, dans `GuestArrivalPanel`, sous "Qui est arrivé ?") : "l'espace où on voit les chaises... devrait être surtout dans la page où on voit l'invité." Nouvelle prop `tableNumber` (la vraie table assignée, `invitations.table_id -> tables.number`, jamais devinée par nom) ; le panneau ne s'affiche que si `TABLE_SEAT_NAMES[tableNumber]` existe, comme sur `/plan-table`. Un bouton 📍 apparaît à côté de chaque membre dont le nom correspond exactement (accents/casse ignorés, jamais une tolérance approchée — deux personnes différentes peuvent avoir des noms très proches, ex. "Andrea Neves" et "André Neves", chacune sur une table différente) à un siège de SA table réelle ; le toucher surligne ce siège et fait défiler jusqu'au dessin. Nouvelle fonction `findSeatIndexByName(tableNumber, name)` dans `lib/floorPlanSeats.ts`. (2) **Sur `/plan-table`, toucher une invitation dans la liste au-dessus du dessin surligne tous ses membres retrouvés d'un coup** au lieu de naviguer vers `/tables/[tableId]` : "j'appuie sur le nom, ça descend en bas... [mais] j'appuie vraiment sur la table, ça m'amène dans la prochaine page" — le `<Link>` de `TableCard` n'englobe plus la liste des invitations quand un nouveau callback `onSelectInvitation` est fourni (uniquement sur la carte de la table sélectionnée ; les grilles de cartes plus bas sur la page gardent l'ancien comportement, callback absent). Recherche par nom exact parmi `inv.nom_affichage` et `extractMembresComplet(inv.notes)` (déjà utilisé ailleurs sur cette page). `components/TableSeatWheel.tsx` généralisé : `highlightedIndex: number | null` devient `highlightedIndices: number[]` (plusieurs sièges à la fois), utilisé par les deux pages. **Bonus demandé au passage** : le bouton "📷 Invité surprise" (gros bouton séparé) devient une simple icône caméra à côté du "+" dans `GuestArrivalPanel`, plus un bouton "Terminé" (doré) qui ramène à `/scan` — "la flèche retour n'est pas intuitive... quand on clique Terminé ça nous retourne sur la page du scanner". Les deux fonctionnalités (ajout direct "+" et parcours photo) restent strictement inchangées, seule leur présentation change. Aucune migration (changements purement client, la vraie source de placement reste `invitations.table_id`).
- v1.48.4 : retour de Gersom après avoir activé les notifications Push sur son iPhone (PR #77 mergé) — question posée : « le petit 1 indicateur sur l'icône avant de l'ouvrir », en clarifiant que la vraie notification système (bannière/écran verrouillé, déjà en place) est différente d'un badge numérique posé directement sur l'icône de l'app, visible même sans ouvrir l'app (Badging API, distinct du badge affiché à l'intérieur de l'app une fois ouverte). **`lib/appBadge.ts`** (nouveau) : `syncAppBadge`/`clearAppBadge`, enveloppe best-effort autour de `navigator.setAppBadge`/`clearAppBadge` (supportée par les PWA installées sur l'écran d'accueil depuis iOS 16.4 ; absente ailleurs, jamais bloquant). Deux chemins de mise à jour : (1) **en tâche de fond**, `public/sw.js` lit désormais `badgeCount` dans le payload Push et appelle la Badging API en plus d'afficher la notification — fonctionne même app fermée, exactement le scénario demandé ; `lib/webPush.ts` (`notifyGuestApprovalReviewers`) calcule ce compte (même valeur que `pending_count`, déjà affiché comme badge interne). (2) **en premier plan**, les trois sondeurs existants (`AccountMenu`/`BottomNav`/`GuestApprovalsShortcut`) recalent aussi ce badge à chaque rafraîchissement du compte d'approbations en attente — corrige/efface le badge dès la réouverture de l'app, sans attendre un futur Push ; effacé explicitement à la déconnexion (`AccountMenu`, un appareil pouvant changer de compte). `tests/app-badge.test.ts` (nouveau) + 4 tests ajoutés à `tests/guest-approvals.test.ts` verrouillent le comportement et le câblage bout en bout.
- v1.48.3 : bug signalé par Gersom (capture d'écran « Reconsidérer et placer ») : « les trois petits points restent là très longtemps... ça prend vraiment du temps à approuver ». QE_QA_PROCESS suivi : reproduction par lecture du code (comportement non reproductible autrement sans vraie latence réseau Twilio/Push), root-cause identifiée puis recherche systématique des cas similaires plutôt qu'un correctif isolé sur le seul écran signalé. **Root cause** : `finalizeDecision` (`lib/guestApprovalDecide.ts`, partagée par la décision normale, `/approve/[token]`, WhatsApp entrant ET la reconsidération) et `app/api/guest-approvals/[id]/assign-table/route.ts` attendaient chacun deux envois indépendants documentés « best-effort » (SMS/WhatsApp de confirmation, Push aux placeurs, rapport SMS aux directeurs de festin) **en séquence** et **sans aucune limite de temps** avant de répondre à l'agent — un `fetch`/`webpush.sendNotification` bloqué pouvait laisser le bouton sur « … » indéfiniment, alors que l'intention documentée était que ces envois ne bloquent jamais la décision (déjà actée en base à ce stade). Corrigé à la source, une seule fois pour tous les appelants : `lib/twilio.ts` borne chaque requête HTTP Twilio à 8s (`AbortSignal.timeout`), `lib/webPush.ts` borne chaque envoi Push à 8s (`options.timeout`, supporté nativement par `web-push`), et les deux couples d'envois indépendants (`finalizeDecision`, `assign-table/route.ts`) tournent désormais en parallèle (`Promise.allSettled`) plutôt qu'un après l'autre. `tests/guest-approvals.test.ts` (3 nouveaux tests) verrouille les deux timeouts et le parallélisme. **Découverte connexe pendant cette revue, sur demande explicite de Gersom** : Twilio (SMS + WhatsApp) est en réalité désactivé intentionnellement pour l'instant (« c'est toggle off... on activera plus tard ») — ajout d'un interrupteur explicite `TWILIO_ENABLED` (`lib/twilio.ts`, `isTwilioEnabled()`), désactivé par défaut, centralisé dans `getTwilioConfig`/`getWhatsAppConfig` pour que `sendSms`/`sendWhatsApp` et tous leurs appelants (`lib/guestApprovalNotify.ts`) s'adaptent automatiquement à son état sans aucun changement de code ailleurs — réactiver plus tard ne demande qu'une seule variable d'environnement (`TWILIO_ENABLED=true`, en plus des identifiants `TWILIO_*` déjà documentés). Tant que désactivé, aucune requête réseau Twilio n'est même tentée (vérifié par test comportemental, pas seulement une inspection du source) : la création/décision/assignation d'un invité surprise reste entièrement fonctionnelle depuis l'application elle-même, seule la notification externe à l'approbateur/au directeur de festin ne part pas. `DEPLOIEMENT.md` et `docs/BUSINESS_RULES.md` documentent le toggle et sa procédure de réactivation.
- v1.48.2 : retour de Gersom (5 photos) sur le panneau « Vu sur le plan photographié » de `/plan-table` — "je veux voir un dessin plutot de chaque tables avec les places... juste une image de la table avec leurs differents noms et leurs sieges et voir aussi les sieges vides. Vraiment comme sur les images" (photos seatplan.io transmises en référence) et "quand on selectionne une personne dans la liste ca doit mettre la chaise de la personne en brillance... comme sur la photo" (exemple d'une carte nominative numérique montrant un siège mis en évidence). **`components/TableSeatWheel.tsx`** (nouveau) remplace l'ancienne grille de boutons (`grid grid-cols-2`) par un vrai dessin SVG circulaire : dix étiquettes de siège rayonnant autour d'un cercle central numéroté, dans le sens horaire depuis le haut, chacune tournée à son propre angle (`transform="rotate(...)"`, même mécanisme que les libellés verticaux de `components/FloorPlan.tsx`) — fidèle aux photos transmises, y compris les étiquettes du bas qui se retrouvent donc la tête en bas (aucune correction de lisibilité). Les sièges vides restent visuellement distincts (contour pointillé, libellé "Vide"). Le siège sélectionné (état `highlightedSeat`, déjà introduit en v1.48.0) est mis en évidence avec les couleurs accent, exactement comme sur l'exemple de carte nominative montré par Gersom. `app/plan-table/page.tsx` : le panneau garde le même conteneur `.card` et le même texte informatif (complété d'une phrase "Touchez un nom pour mettre son siège en évidence."), seul son contenu change — toujours **purement informatif**, jamais une source de placement (`lib/floorPlanSeats.ts` inchangé). `tests/table-seat-wheel.test.ts` (nouveau).
- v1.48.1 : correctif du même jour, retour de Gersom sur capture d'écran de l'app rendue — "tu as inversé la zone sud et la zone nord... prends les 22 tables en bas, monte-les dans le nord, prends les 20 en haut, mets-les en bas... garder l'alignement... deux tables qui sont justement dans le nord-ouest (22 et 23)". Le bloc à 22 tables (6×4, avec la paire 22/23) et le bloc à 20 tables (5×4 "propre") étaient positionnés à l'envers en v1.48.0. Corrigé dans `components/FloorPlan.tsx` en permutant uniquement la bande de rangées Y entre les deux blocs (mêmes colonnes X qu'avant pour chaque table, donc même alignement/mêmes voisins) : aucune position redevinée, seul le bloc entier change de bande verticale. La paire 22/23 (première colonne du bloc nord, rangées 1-2) est bien au nord-ouest désormais. `FLOOR_PLAN_ZONE_LABELS` n'a pas eu besoin de changer (sa position dépend de la bande Y haut/bas, pas des tables qui s'y trouvent).


## Reprise rapide pour Claude AI

1. Commencer par `git fetch origin main` et comparer `HEAD` à `origin/main`; ne jamais supposer qu'un diff transmis est encore manquant.
2. Vérifier `package.json` : la version attendue au moment de cette transmission est `1.53.7`.
3. Pour les approbations, lire ensemble `app/approbations/page.tsx`, `app/approbations/[id]/assign/page.tsx`, `lib/guestApprovalDecide.ts`, `lib/webPush.ts` et les migrations `0038` et `0050`.
4. Pour la navigation, modifier la source centralisée `components/BottomNav.tsx`; ne pas recopier des menus dans les pages.
5. Ne pas modifier la règle des rôles sans mettre à jour `lib/permissions.ts`, les routes API, `tests/permissions.test.ts`, `tests/guest-approvals.test.ts` et `docs/BUSINESS_RULES.md` dans le même lot.
6. Avant livraison : `npx tsc --noEmit`, toutes les suites `node --test tests/*.test.ts`, `npm run build`, puis `git diff --check`.
7. Les prochains changements doivent passer par une branche et une PR pour permettre la révision avant fusion; ne pousser directement sur `main` que si Gersom le demande explicitement.
